{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"NewtonianPropagator","text":"<p>High-Fidelity 6-DOF/7-DOF Astrodynamics &amp; Multi-Body Simulation Engine.</p> <p>Select API Reference in the navigation menu to browse the module documentation.</p>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>Foundations<ul> <li>constants</li> <li>ephemeris</li> <li>time</li> </ul> </li> <li>Mission Design<ul> <li>cr3bp</li> <li>flyby</li> <li>lambert</li> </ul> </li> <li>Dynamics &amp; Propagation<ul> <li>forces</li> <li>integrators</li> <li>propagator</li> </ul> </li> <li>Constellations &amp; Launch<ul> <li>launchers</li> <li>swarm</li> </ul> </li> </ul>"},{"location":"reference/constellations_launch/launchers/","title":"launchers","text":""},{"location":"reference/constellations_launch/launchers/#core.launchers","title":"core.launchers","text":"<p>Launch Vehicle Performance &amp; Payload Injection Capacity Engine.</p> <p>Provides empirical characteristic launch energy (\\(C_3\\)) curves, stage mass breakdowns, Tsiolkovsky rocket equation models, and catalog specifications for active and legacy launch vehicles.</p>"},{"location":"reference/constellations_launch/launchers/#core.launchers.StageSpec","title":"StageSpec  <code>dataclass</code>","text":"<pre><code>StageSpec(\n    name: str,\n    propellant_mass_kg: float,\n    dry_mass_kg: float,\n    isp_vac_s: float,\n    thrust_vac_n: float,\n)\n</code></pre> <p>Individual propulsion stage specification.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Stage designation identifier.</p> required <code>propellant_mass_kg</code> <code>float</code> <p>Total usable onboard propellant mass [\\(\\text{kg}\\)].</p> required <code>dry_mass_kg</code> <code>float</code> <p>Structural stage burnout dry mass [\\(\\text{kg}\\)].</p> required <code>isp_vac_s</code> <code>float</code> <p>Vacuum specific impulse \\(I_{\\text{sp}}\\) [\\(\\text{s}\\)].</p> required <code>thrust_vac_n</code> <code>float</code> <p>Total vacuum engine thrust rating [\\(\\text{N}\\)].</p> required"},{"location":"reference/constellations_launch/launchers/#core.launchers.StageSpec.total_mass_kg","title":"total_mass_kg  <code>property</code>","text":"<pre><code>total_mass_kg: float\n</code></pre> <p>Total initial wet mass of the stage in \\(\\text{kg}\\).</p>"},{"location":"reference/constellations_launch/launchers/#core.launchers.StageSpec.stage_delta_v","title":"stage_delta_v","text":"<pre><code>stage_delta_v(payload_mass_kg: float) -&gt; float\n</code></pre> <p>Calculate ideal vacuum Delta-V via the Tsiolkovsky Rocket Equation.</p> \\[ \\Delta V = g_0 I_{\\text{sp}} \\ln\\left(\\frac{m_{\\text{initial}}}{m_{\\text{final}}}\\right) \\] <p>Parameters:</p> Name Type Description Default <code>payload_mass_kg</code> <code>float</code> <p>Net payload mass mounted atop this stage [\\(\\text{kg}\\)].</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Achievable ideal velocity increment in \\(\\text{m/s}\\).</p> Source code in <code>core/launchers.py</code> <pre><code>def stage_delta_v(self, payload_mass_kg: float) -&gt; float:\n    r\"\"\"Calculate ideal vacuum Delta-V via the Tsiolkovsky Rocket Equation.\n\n    $$\n    \\Delta V = g_0 I_{\\text{sp}} \\ln\\left(\\frac{m_{\\text{initial}}}{m_{\\text{final}}}\\right)\n    $$\n\n    Args:\n        payload_mass_kg: Net payload mass mounted atop this stage [$\\text{kg}$].\n\n    Returns:\n        float: Achievable ideal velocity increment in $\\text{m/s}$.\n    \"\"\"\n    m_initial = self.total_mass_kg + payload_mass_kg\n    m_final = self.dry_mass_kg + payload_mass_kg\n    if m_final &lt;= 0 or m_initial &lt;= m_final:\n        return 0.0\n    return float(G0 * self.isp_vac_s * np.log(m_initial / m_final))\n</code></pre>"},{"location":"reference/constellations_launch/launchers/#core.launchers.LaunchVehicle","title":"LaunchVehicle  <code>dataclass</code>","text":"<pre><code>LaunchVehicle(\n    name: str,\n    operator: str,\n    status: str,\n    leo_capacity_kg: float,\n    gto_capacity_kg: float,\n    tli_capacity_kg: Optional[float] = None,\n    max_c3_km2_s2: float = 100.0,\n    c3_poly_coeffs: Optional[\n        Tuple[float, float, float]\n    ] = None,\n    stages: List[StageSpec] = list(),\n)\n</code></pre> <p>Launch vehicle model with payload injection curves and stage architectures.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Full vehicle model name.</p> required <code>operator</code> <code>str</code> <p>Operating launch provider or agency.</p> required <code>status</code> <code>str</code> <p>Operational status (<code>'active'</code> or <code>'legacy'</code>).</p> required <code>leo_capacity_kg</code> <code>float</code> <p>Payload capability to Low Earth Orbit [\\(\\text{kg}\\)].</p> required <code>gto_capacity_kg</code> <code>float</code> <p>Payload capability to Geostationary Transfer Orbit [\\(\\text{kg}\\)].</p> required <code>tli_capacity_kg</code> <code>Optional[float]</code> <p>Payload capability to Trans-Lunar Injection [\\(\\text{kg}\\)].</p> <code>None</code> <code>max_c3_km2_s2</code> <code>float</code> <p>Maximum characteristic launch energy cut-off [\\(\\text{km}^2 / \\text{s}^2\\)].</p> <code>100.0</code> <code>c3_poly_coeffs</code> <code>Optional[Tuple[float, float, float]]</code> <p>Polynomial coefficients \\((c_2, c_1, c_0)\\) modeling payload mass as a function of \\(C_3\\): \\(m(C_3) = c_2 C_3^2 + c_1 C_3 + c_0\\).</p> <code>None</code> <code>stages</code> <code>List[StageSpec]</code> <p>Ordered sequence of stages from booster to upper stage.</p> <code>list()</code>"},{"location":"reference/constellations_launch/launchers/#core.launchers.LaunchVehicle.payload_for_c3","title":"payload_for_c3","text":"<pre><code>payload_for_c3(c3_km2_s2: float) -&gt; float\n</code></pre> <p>Compute maximum deliverable payload mass for a given \\(C_3\\) energy.</p> <p>Parameters:</p> Name Type Description Default <code>c3_km2_s2</code> <code>float</code> <p>Characteristic launch energy \\(C_3 = v_\\infty^2\\) in \\(\\text{km}^2 / \\text{s}^2\\).</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Maximum deliverable payload mass in \\(\\text{kg}\\).</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>If <code>c3_poly_coeffs</code> is not defined for this vehicle.</p> Source code in <code>core/launchers.py</code> <pre><code>def payload_for_c3(self, c3_km2_s2: float) -&gt; float:\n    r\"\"\"Compute maximum deliverable payload mass for a given $C_3$ energy.\n\n    Args:\n        c3_km2_s2: Characteristic launch energy $C_3 = v_\\infty^2$ in $\\text{km}^2 / \\text{s}^2$.\n\n    Returns:\n        float: Maximum deliverable payload mass in $\\text{kg}$.\n\n    Raises:\n        NotImplementedError: If `c3_poly_coeffs` is not defined for this vehicle.\n    \"\"\"\n    if self.c3_poly_coeffs is None:\n        raise NotImplementedError(f\"C3 polynomial model is not defined for {self.name}.\")\n\n    if c3_km2_s2 &lt; 0.0:\n        c3_km2_s2 = 0.0\n\n    if c3_km2_s2 &gt; self.max_c3_km2_s2:\n        return 0.0\n\n    mass = float(np.polyval(self.c3_poly_coeffs, c3_km2_s2))\n    return float(max(0.0, mass))\n</code></pre>"},{"location":"reference/constellations_launch/launchers/#core.launchers.LaunchVehicle.total_vehicle_delta_v","title":"total_vehicle_delta_v","text":"<pre><code>total_vehicle_delta_v(payload_mass_kg: float) -&gt; float\n</code></pre> <p>Compute ideal multi-stage Delta-V through sequential stage burn/jettison.</p> <p>Parameters:</p> Name Type Description Default <code>payload_mass_kg</code> <code>float</code> <p>Net payload mass carried atop the upper stage [\\(\\text{kg}\\)].</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Total achievable velocity increment in \\(\\text{m/s}\\).</p> Source code in <code>core/launchers.py</code> <pre><code>def total_vehicle_delta_v(self, payload_mass_kg: float) -&gt; float:\n    r\"\"\"Compute ideal multi-stage Delta-V through sequential stage burn/jettison.\n\n    Args:\n        payload_mass_kg: Net payload mass carried atop the upper stage [$\\text{kg}$].\n\n    Returns:\n        float: Total achievable velocity increment in $\\text{m/s}$.\n    \"\"\"\n    if not self.stages:\n        return 0.0\n\n    total_dv = 0.0\n    current_payload = payload_mass_kg\n\n    for i in reversed(range(len(self.stages))):\n        stage = self.stages[i]\n        dv_stage = stage.stage_delta_v(current_payload)\n        total_dv += dv_stage\n        current_payload += stage.total_mass_kg\n\n    return total_dv\n</code></pre>"},{"location":"reference/constellations_launch/launchers/#core.launchers.get_launcher","title":"get_launcher","text":"<pre><code>get_launcher(vehicle_key: str) -&gt; LaunchVehicle\n</code></pre> <p>Retrieve a <code>LaunchVehicle</code> instance from the catalog by key.</p> <p>Parameters:</p> Name Type Description Default <code>vehicle_key</code> <code>str</code> <p>Catalog lookup key (e.g., <code>'falcon_9_reusable'</code>, <code>'isro_lvm3'</code>).</p> required <p>Returns:</p> Name Type Description <code>LaunchVehicle</code> <code>LaunchVehicle</code> <p>The matching vehicle specification instance.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If <code>vehicle_key</code> is not found in the vehicle catalog.</p> Source code in <code>core/launchers.py</code> <pre><code>def get_launcher(vehicle_key: str) -&gt; LaunchVehicle:\n    r\"\"\"Retrieve a `LaunchVehicle` instance from the catalog by key.\n\n    Args:\n        vehicle_key: Catalog lookup key (e.g., `'falcon_9_reusable'`, `'isro_lvm3'`).\n\n    Returns:\n        LaunchVehicle: The matching vehicle specification instance.\n\n    Raises:\n        KeyError: If `vehicle_key` is not found in the vehicle catalog.\n    \"\"\"\n    key = vehicle_key.lower().strip()\n    if key not in LAUNCH_VEHICLE_CATALOG:\n        available = \", \".join(LAUNCH_VEHICLE_CATALOG.keys())\n        raise KeyError(f\"Launcher '{vehicle_key}' not found. Available launchers: {available}\")\n    return LAUNCH_VEHICLE_CATALOG[key]\n</code></pre>"},{"location":"reference/constellations_launch/launchers/#core.launchers.list_available_launchers","title":"list_available_launchers","text":"<pre><code>list_available_launchers() -&gt; List[Dict[str, str]]\n</code></pre> <p>Retrieve a summary list of all cataloged launch vehicles.</p> <p>Returns:</p> Type Description <code>List[Dict[str, str]]</code> <p>List[Dict[str, str]]: Dictionaries containing launcher summaries with keys: <code>'key'</code>, <code>'name'</code>, <code>'operator'</code>, and <code>'status'</code>.</p> Source code in <code>core/launchers.py</code> <pre><code>def list_available_launchers() -&gt; List[Dict[str, str]]:\n    r\"\"\"Retrieve a summary list of all cataloged launch vehicles.\n\n    Returns:\n        List[Dict[str, str]]: Dictionaries containing launcher summaries with keys:\n            `'key'`, `'name'`, `'operator'`, and `'status'`.\n    \"\"\"\n    return [\n        {\"key\": key, \"name\": lv.name, \"operator\": lv.operator, \"status\": lv.status}\n        for key, lv in LAUNCH_VEHICLE_CATALOG.items()\n    ]\n</code></pre>"},{"location":"reference/constellations_launch/swarm/","title":"swarm","text":""},{"location":"reference/constellations_launch/swarm/#core.swarm","title":"core.swarm","text":"<p>Multi-Agent Swarm Propagator for constellation &amp; formation flying.</p> <p>Facilitates simultaneous constellation trajectory integration, Chief-Deputy relative tracking, and Local-Vertical/Local-Horizontal (LVLH) coordinate transformations.</p>"},{"location":"reference/constellations_launch/swarm/#core.swarm.SwarmPropagator","title":"SwarmPropagator","text":"<pre><code>SwarmPropagator(chief_propagator: SpacecraftPropagator)\n</code></pre> <p>Multi-satellite formation trajectory propagator.</p> <p>Parameters:</p> Name Type Description Default <code>chief_propagator</code> <code>SpacecraftPropagator</code> <p>Configured baseline propagator engine assigned to the lead (Chief) spacecraft.</p> required Source code in <code>core/swarm.py</code> <pre><code>def __init__(self, chief_propagator: SpacecraftPropagator):\n    self.chief_prop = chief_propagator\n    self.deputies: List[Dict] = []\n</code></pre>"},{"location":"reference/constellations_launch/swarm/#core.swarm.SwarmPropagator.add_deputy","title":"add_deputy","text":"<pre><code>add_deputy(\n    name: str,\n    propagator: SpacecraftPropagator,\n    r0: ndarray,\n    v0: ndarray,\n) -&gt; None\n</code></pre> <p>Register a deputy satellite agent within the constellation.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Unique deputy agent identifier.</p> required <code>propagator</code> <code>SpacecraftPropagator</code> <p>Dedicated propagator instance configured with specific force models for this agent.</p> required <code>r0</code> <code>ndarray</code> <p>Initial inertial position vector \\([x, y, z]\\) in meters.</p> required <code>v0</code> <code>ndarray</code> <p>Initial inertial velocity vector \\([v_x, v_y, v_z]\\) in meters/second.</p> required Source code in <code>core/swarm.py</code> <pre><code>def add_deputy(\n    self,\n    name: str,\n    propagator: SpacecraftPropagator,\n    r0: np.ndarray,\n    v0: np.ndarray\n) -&gt; None:\n    r\"\"\"Register a deputy satellite agent within the constellation.\n\n    Args:\n        name: Unique deputy agent identifier.\n        propagator: Dedicated propagator instance configured with specific force models for this agent.\n        r0: Initial inertial position vector $[x, y, z]$ in meters.\n        v0: Initial inertial velocity vector $[v_x, v_y, v_z]$ in meters/second.\n    \"\"\"\n    self.deputies.append({\n        \"name\": name,\n        \"prop\": propagator,\n        \"r0\": r0,\n        \"v0\": v0\n    })\n</code></pre>"},{"location":"reference/constellations_launch/swarm/#core.swarm.SwarmPropagator.plot_relative_motion","title":"plot_relative_motion  <code>staticmethod</code>","text":"<pre><code>plot_relative_motion(\n    relative_tracks: Dict[str, ndarray],\n    title: str = \"Swarm Relative LVLH Motion\",\n) -&gt; None\n</code></pre> <p>Render a 3D visualization in the Chief-centered rotating LVLH frame.</p> <p>Parameters:</p> Name Type Description Default <code>relative_tracks</code> <code>Dict[str, ndarray]</code> <p>Dictionary mapping deputy identifiers to <code>(N, 3)</code> relative coordinate arrays.</p> required <code>title</code> <code>str</code> <p>Display title for the generated figure. Defaults to <code>'Swarm Relative LVLH Motion'</code>.</p> <code>'Swarm Relative LVLH Motion'</code> Source code in <code>core/swarm.py</code> <pre><code>@staticmethod\ndef plot_relative_motion(\n    relative_tracks: Dict[str, np.ndarray],\n    title: str = \"Swarm Relative LVLH Motion\"\n) -&gt; None:\n    r\"\"\"Render a 3D visualization in the Chief-centered rotating LVLH frame.\n\n    Args:\n        relative_tracks: Dictionary mapping deputy identifiers to `(N, 3)` relative coordinate arrays.\n        title: Display title for the generated figure. Defaults to `'Swarm Relative LVLH Motion'`.\n    \"\"\"\n    fig = plt.figure(figsize=(10.0, 8.0))\n    ax = fig.add_subplot(111, projection='3d')\n\n    ax.scatter(0, 0, 0, color='gold', s=120, edgecolors='black', label='Chief (Origin)', zorder=10)\n\n    colors = ['dodgerblue', 'crimson', 'forestgreen', 'darkviolet', 'darkorange']\n    for idx, (name, track) in enumerate(relative_tracks.items()):\n        color = colors[idx % len(colors)]\n        ax.plot(track[:, 1], track[:, 0], track[:, 2], color=color, linewidth=1.5, label=f'{name} Path')\n        ax.scatter(track[0, 1], track[0, 0], track[0, 2], color=color, marker='o', s=40, label=f'{name} Start')\n        ax.scatter(track[-1, 1], track[-1, 0], track[-1, 2], color=color, marker='x', s=50, label=f'{name} End')\n\n    ax.set_xlabel('Along-Track / In-Track [y] (m)')\n    ax.set_ylabel('Radial [x] (m)')\n    ax.set_zlabel('Cross-Track [z] (m)')\n    ax.set_title(title)\n    ax.grid(True, linestyle=':', alpha=0.6)\n    ax.legend()\n    plt.tight_layout()\n    plt.show()\n</code></pre>"},{"location":"reference/constellations_launch/swarm/#core.swarm.SwarmPropagator.propagate_swarm","title":"propagate_swarm","text":"<pre><code>propagate_swarm(\n    r0_chief: ndarray,\n    v0_chief: ndarray,\n    t_span: float,\n    dt: float,\n) -&gt; Tuple[ndarray, ndarray, Dict[str, ndarray]]\n</code></pre> <p>Propagate Chief and all Deputy spacecraft simultaneously.</p> <p>Parameters:</p> Name Type Description Default <code>r0_chief</code> <code>ndarray</code> <p>Initial Chief position vector \\([x, y, z]\\) in meters.</p> required <code>v0_chief</code> <code>ndarray</code> <p>Initial Chief velocity vector \\([v_x, v_y, v_z]\\) in meters/second.</p> required <code>t_span</code> <code>float</code> <p>Total propagation duration in seconds.</p> required <code>dt</code> <code>float</code> <p>Fixed integration step size in seconds.</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray, Dict[str, ndarray]]</code> <p>Tuple[np.ndarray, np.ndarray, Dict[str, np.ndarray]]: A tuple containing: - times (<code>np.ndarray</code>): 1D array of time steps of shape <code>(N,)</code> [\\(\\text{s}\\)]. - chief_states (<code>np.ndarray</code>): Array of Chief ECI states of shape <code>(N, 6)</code>. - relative_positions (<code>Dict[str, np.ndarray]</code>): Mapping of deputy names   to relative LVLH track coordinates of shape <code>(N, 3)</code> [\\(\\text{m}\\)].</p> Source code in <code>core/swarm.py</code> <pre><code>def propagate_swarm(\n    self,\n    r0_chief: np.ndarray,\n    v0_chief: np.ndarray,\n    t_span: float,\n    dt: float\n) -&gt; Tuple[np.ndarray, np.ndarray, Dict[str, np.ndarray]]:\n    r\"\"\"Propagate Chief and all Deputy spacecraft simultaneously.\n\n    Args:\n        r0_chief: Initial Chief position vector $[x, y, z]$ in meters.\n        v0_chief: Initial Chief velocity vector $[v_x, v_y, v_z]$ in meters/second.\n        t_span: Total propagation duration in seconds.\n        dt: Fixed integration step size in seconds.\n\n    Returns:\n        Tuple[np.ndarray, np.ndarray, Dict[str, np.ndarray]]: A tuple containing:\n            - **times** (`np.ndarray`): 1D array of time steps of shape `(N,)` [$\\text{s}$].\n            - **chief_states** (`np.ndarray`): Array of Chief ECI states of shape `(N, 6)`.\n            - **relative_positions** (`Dict[str, np.ndarray]`): Mapping of deputy names\n              to relative LVLH track coordinates of shape `(N, 3)` [$\\text{m}$].\n    \"\"\"\n    times, chief_states = self.chief_prop.propagate(r0_chief, v0_chief, t_span, dt)\n\n    relative_tracks = {}\n    for dep in self.deputies:\n        _, dep_states = dep[\"prop\"].propagate(dep[\"r0\"], dep[\"v0\"], t_span, dt)\n\n        num_steps = len(times)\n        rel_lvlh = np.zeros((num_steps, 3))\n\n        for i in range(num_steps):\n            r_c = chief_states[i, 0:3]\n            v_c = chief_states[i, 3:6]\n            r_d = dep_states[i, 0:3]\n            rel_lvlh[i] = eci_to_lvlh(r_c, v_c, r_d)\n\n        relative_tracks[dep[\"name\"]] = rel_lvlh\n\n    return times, chief_states, relative_tracks\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/","title":"forces","text":""},{"location":"reference/dynamics_propagation/forces/#core.forces","title":"core.forces","text":"<p>Acceleration models for the Newtonian superposition summation junction.</p> <p>All acceleration models operate in the Earth-Centered Inertial (ECI) coordinate frame with position vectors \\(\\mathbf{r} \\in \\mathbb{R}^3\\text{ [m]}\\) and velocity vectors \\(\\mathbf{v} \\in \\mathbb{R}^3\\text{ [m/s]}\\).</p>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_earth_gravity","title":"accel_earth_gravity","text":"<pre><code>accel_earth_gravity(r: ndarray) -&gt; ndarray\n</code></pre> <p>Compute primary central-body Newtonian point-mass gravitational acceleration.</p> \\[ \\mathbf{a}_{\\text{grav}} = -\\frac{\\mu_{\\oplus}}{\\|\\mathbf{r}\\|^3} \\mathbf{r} \\] <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Spacecraft position vector \\(\\mathbf{r}\\) in the ECI frame [\\(\\text{m}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Gravitational acceleration vector \\(\\mathbf{a}_{\\text{grav}}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_earth_gravity(r: np.ndarray) -&gt; np.ndarray:\n    r\"\"\"Compute primary central-body Newtonian point-mass gravitational acceleration.\n\n    $$\n    \\mathbf{a}_{\\text{grav}} = -\\frac{\\mu_{\\oplus}}{\\|\\mathbf{r}\\|^3} \\mathbf{r}\n    $$\n\n    Args:\n        r: Spacecraft position vector $\\mathbf{r}$ in the ECI frame [$\\text{m}$].\n\n    Returns:\n        np.ndarray: Gravitational acceleration vector $\\mathbf{a}_{\\text{grav}}$ [$\\text{m/s}^2$].\n    \"\"\"\n    r_mag = np.linalg.norm(r)\n    return -G_EARTH * r / (r_mag ** 3)\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_j2_perturbation","title":"accel_j2_perturbation","text":"<pre><code>accel_j2_perturbation(r: ndarray) -&gt; ndarray\n</code></pre> <p>Compute Earth oblateness (\\(J_2\\) zonal harmonic) gravitational perturbation.</p> \\[ \\mathbf{a}_{J_2} = \\frac{3 J_2 \\mu_{\\oplus} R_{\\oplus}^2}{2 \\|\\mathbf{r}\\|^5} \\begin{bmatrix} x \\left(5 \\frac{z^2}{\\|\\mathbf{r}\\|^2} - 1\\right) \\\\ y \\left(5 \\frac{z^2}{\\|\\mathbf{r}\\|^2} - 1\\right) \\\\ z \\left(5 \\frac{z^2}{\\|\\mathbf{r}\\|^2} - 3\\right) \\end{bmatrix} \\] <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Spacecraft position vector \\(\\mathbf{r} = [x, y, z]^T\\) in the ECI frame [\\(\\text{m}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Perturbation acceleration vector \\(\\mathbf{a}_{J_2}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_j2_perturbation(r: np.ndarray) -&gt; np.ndarray:\n    r\"\"\"Compute Earth oblateness ($J_2$ zonal harmonic) gravitational perturbation.\n\n    $$\n    \\mathbf{a}_{J_2} = \\frac{3 J_2 \\mu_{\\oplus} R_{\\oplus}^2}{2 \\|\\mathbf{r}\\|^5}\n    \\begin{bmatrix}\n    x \\left(5 \\frac{z^2}{\\|\\mathbf{r}\\|^2} - 1\\right) \\\\\n    y \\left(5 \\frac{z^2}{\\|\\mathbf{r}\\|^2} - 1\\right) \\\\\n    z \\left(5 \\frac{z^2}{\\|\\mathbf{r}\\|^2} - 3\\right)\n    \\end{bmatrix}\n    $$\n\n    Args:\n        r: Spacecraft position vector $\\mathbf{r} = [x, y, z]^T$ in the ECI frame [$\\text{m}$].\n\n    Returns:\n        np.ndarray: Perturbation acceleration vector $\\mathbf{a}_{J_2}$ [$\\text{m/s}^2$].\n    \"\"\"\n    x, y, z = r\n    r_mag = np.linalg.norm(r)\n    factor = 1.5 * J2_EARTH * G_EARTH * (R_EARTH ** 2) / (r_mag ** 5)\n    z_sq = (z / r_mag) ** 2\n    return factor * np.array([\n        x * (5.0 * z_sq - 1.0),\n        y * (5.0 * z_sq - 1.0),\n        z * (5.0 * z_sq - 3.0)\n    ])\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_j3_perturbation","title":"accel_j3_perturbation","text":"<pre><code>accel_j3_perturbation(r: ndarray) -&gt; ndarray\n</code></pre> <p>Compute Earth pear-shape asymmetry (\\(J_3\\) zonal harmonic) perturbation.</p> <p>With \\(s = \\sin\\phi = \\frac{z}{\\|\\mathbf{r}\\|}\\):</p> \\[ \\mathbf{a}_{J_3} = \\frac{1}{2} \\frac{J_3 \\mu_{\\oplus} R_{\\oplus}^3}{\\|\\mathbf{r}\\|^5} \\begin{bmatrix} 5 x \\left(7s^3 - 3s\\right) \\\\ 5 y \\left(7s^3 - 3s\\right) \\\\ \\|\\mathbf{r}\\| \\left(35s^4 - 30s^2 + 3\\right) \\end{bmatrix} \\] <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Spacecraft position vector \\(\\mathbf{r} = [x, y, z]^T\\) in the ECI frame [\\(\\text{m}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Perturbation acceleration vector \\(\\mathbf{a}_{J_3}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_j3_perturbation(r: np.ndarray) -&gt; np.ndarray:\n    r\"\"\"Compute Earth pear-shape asymmetry ($J_3$ zonal harmonic) perturbation.\n\n    With $s = \\sin\\phi = \\frac{z}{\\|\\mathbf{r}\\|}$:\n\n    $$\n    \\mathbf{a}_{J_3} = \\frac{1}{2} \\frac{J_3 \\mu_{\\oplus} R_{\\oplus}^3}{\\|\\mathbf{r}\\|^5}\n    \\begin{bmatrix}\n    5 x \\left(7s^3 - 3s\\right) \\\\\n    5 y \\left(7s^3 - 3s\\right) \\\\\n    \\|\\mathbf{r}\\| \\left(35s^4 - 30s^2 + 3\\right)\n    \\end{bmatrix}\n    $$\n\n    Args:\n        r: Spacecraft position vector $\\mathbf{r} = [x, y, z]^T$ in the ECI frame [$\\text{m}$].\n\n    Returns:\n        np.ndarray: Perturbation acceleration vector $\\mathbf{a}_{J_3}$ [$\\text{m/s}^2$].\n    \"\"\"\n    x, y, z = r\n    r_mag = np.linalg.norm(r)\n    s = z / r_mag\n\n    factor = 0.5 * J3_EARTH * G_EARTH * (R_EARTH ** 3) / (r_mag ** 5)\n\n    ax = 5.0 * factor * x * (7.0 * (s ** 3) - 3.0 * s)\n    ay = 5.0 * factor * y * (7.0 * (s ** 3) - 3.0 * s)\n    az = factor * (35.0 * (s ** 4) - 30.0 * (s ** 2) + 3.0) * r_mag\n\n    return np.array([ax, ay, az])\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_j4_perturbation","title":"accel_j4_perturbation","text":"<pre><code>accel_j4_perturbation(r: ndarray) -&gt; ndarray\n</code></pre> <p>Compute Earth second-order oblateness (\\(J_4\\) zonal harmonic) perturbation.</p> <p>With \\(s = \\sin\\phi = \\frac{z}{\\|\\mathbf{r}\\|}\\):</p> \\[ \\mathbf{a}_{J_4} = \\frac{5}{8} \\frac{J_4 \\mu_{\\oplus} R_{\\oplus}^4}{\\|\\mathbf{r}\\|^6} \\begin{bmatrix} x \\left(3 - 42s^2 + 63s^4\\right) \\\\ y \\left(3 - 42s^2 + 63s^4\\right) \\\\ z \\left(15 - 70s^2 + 63s^4\\right) \\end{bmatrix} \\] <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Spacecraft position vector \\(\\mathbf{r} = [x, y, z]^T\\) in the ECI frame [\\(\\text{m}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Perturbation acceleration vector \\(\\mathbf{a}_{J_4}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_j4_perturbation(r: np.ndarray) -&gt; np.ndarray:\n    r\"\"\"Compute Earth second-order oblateness ($J_4$ zonal harmonic) perturbation.\n\n    With $s = \\sin\\phi = \\frac{z}{\\|\\mathbf{r}\\|}$:\n\n    $$\n    \\mathbf{a}_{J_4} = \\frac{5}{8} \\frac{J_4 \\mu_{\\oplus} R_{\\oplus}^4}{\\|\\mathbf{r}\\|^6}\n    \\begin{bmatrix}\n    x \\left(3 - 42s^2 + 63s^4\\right) \\\\\n    y \\left(3 - 42s^2 + 63s^4\\right) \\\\\n    z \\left(15 - 70s^2 + 63s^4\\right)\n    \\end{bmatrix}\n    $$\n\n    Args:\n        r: Spacecraft position vector $\\mathbf{r} = [x, y, z]^T$ in the ECI frame [$\\text{m}$].\n\n    Returns:\n        np.ndarray: Perturbation acceleration vector $\\mathbf{a}_{J_4}$ [$\\text{m/s}^2$].\n    \"\"\"\n    x, y, z = r\n    r_mag = np.linalg.norm(r)\n    s = z / r_mag\n\n    factor = (5.0 / 8.0) * J4_EARTH * G_EARTH * (R_EARTH ** 4) / (r_mag ** 6)\n\n    term_xy = 3.0 - 42.0 * (s ** 2) + 63.0 * (s ** 4)\n    term_z = 15.0 - 70.0 * (s ** 2) + 63.0 * (s ** 4)\n\n    ax = factor * x * term_xy\n    ay = factor * y * term_xy\n    az = factor * z * term_z\n\n    return np.array([ax, ay, az])\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.rv_to_keplerian","title":"rv_to_keplerian","text":"<pre><code>rv_to_keplerian(\n    r: ndarray, v: ndarray, mu: float = G_EARTH\n) -&gt; Dict[str, float]\n</code></pre> <p>Convert Cartesian state vectors \\((\\mathbf{r}, \\mathbf{v})\\) to Classical Orbital Elements (COE).</p> <p>Computes specific orbital energy \\(\\varepsilon\\), specific angular momentum vector \\(\\mathbf{h}\\), and eccentricity vector \\(\\mathbf{e}\\):</p> \\[ \\varepsilon = \\frac{\\|\\mathbf{v}\\|^2}{2} - \\frac{\\mu}{\\|\\mathbf{r}\\|}, \\quad a = -\\frac{\\mu}{2\\varepsilon}, \\quad \\mathbf{h} = \\mathbf{r} \\times \\mathbf{v}, \\quad \\mathbf{e} = \\frac{1}{\\mu} \\left[\\left(\\|\\mathbf{v}\\|^2 - \\frac{\\mu}{\\|\\mathbf{r}\\|}\\right)\\mathbf{r} - (\\mathbf{r} \\cdot \\mathbf{v})\\mathbf{v}\\right] \\] <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Position vector \\(\\mathbf{r}\\) in the ECI frame [\\(\\text{m}\\)].</p> required <code>v</code> <code>ndarray</code> <p>Velocity vector \\(\\mathbf{v}\\) in the ECI frame [\\(\\text{m/s}\\)].</p> required <code>mu</code> <code>float</code> <p>Central body gravitational parameter [\\(\\text{m}^3/\\text{s}^2\\)]. Defaults to Earth (\\(G_{\\text{EARTH}}\\)).</p> <code>G_EARTH</code> <p>Returns:</p> Type Description <code>Dict[str, float]</code> <p>Dict[str, float]: Classical Keplerian orbital elements containing: * <code>a</code>: Semi-major axis \\(a\\) [\\(\\text{m}\\)]. * <code>e</code>: Eccentricity \\(e\\) [dimensionless]. * <code>inc_deg</code>: Inclination \\(i\\) [\\(\\text{deg}\\)]. * <code>raan_deg</code>: Right Ascension of the Ascending Node \\(\\Omega\\) [\\(\\text{deg}\\)]. * <code>argp_deg</code>: Argument of Perigee \\(\\omega\\) [\\(\\text{deg}\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def rv_to_keplerian(r: np.ndarray, v: np.ndarray, mu: float = G_EARTH) -&gt; Dict[str, float]:\n    r\"\"\"Convert Cartesian state vectors $(\\mathbf{r}, \\mathbf{v})$ to Classical Orbital Elements (COE).\n\n    Computes specific orbital energy $\\varepsilon$, specific angular momentum vector $\\mathbf{h}$,\n    and eccentricity vector $\\mathbf{e}$:\n\n    $$\n    \\varepsilon = \\frac{\\|\\mathbf{v}\\|^2}{2} - \\frac{\\mu}{\\|\\mathbf{r}\\|}, \\quad\n    a = -\\frac{\\mu}{2\\varepsilon}, \\quad\n    \\mathbf{h} = \\mathbf{r} \\times \\mathbf{v}, \\quad\n    \\mathbf{e} = \\frac{1}{\\mu} \\left[\\left(\\|\\mathbf{v}\\|^2 - \\frac{\\mu}{\\|\\mathbf{r}\\|}\\right)\\mathbf{r} - (\\mathbf{r} \\cdot \\mathbf{v})\\mathbf{v}\\right]\n    $$\n\n    Args:\n        r: Position vector $\\mathbf{r}$ in the ECI frame [$\\text{m}$].\n        v: Velocity vector $\\mathbf{v}$ in the ECI frame [$\\text{m/s}$].\n        mu: Central body gravitational parameter [$\\text{m}^3/\\text{s}^2$]. Defaults to Earth ($G_{\\text{EARTH}}$).\n\n    Returns:\n        Dict[str, float]: Classical Keplerian orbital elements containing:\n            * `a`: Semi-major axis $a$ [$\\text{m}$].\n            * `e`: Eccentricity $e$ [dimensionless].\n            * `inc_deg`: Inclination $i$ [$\\text{deg}$].\n            * `raan_deg`: Right Ascension of the Ascending Node $\\Omega$ [$\\text{deg}$].\n            * `argp_deg`: Argument of Perigee $\\omega$ [$\\text{deg}$].\n    \"\"\"\n    r_mag = np.linalg.norm(r)\n    v_mag = np.linalg.norm(v)\n\n    h_vec = np.cross(r, v)\n    h_mag = np.linalg.norm(h_vec)\n\n    energy = 0.5 * (v_mag ** 2) - (mu / r_mag)\n    a = -mu / (2.0 * energy) if abs(energy) &gt; 1e-12 else np.nan\n\n    e_vec = (1.0 / mu) * ((v_mag ** 2 - mu / r_mag) * r - np.dot(r, v) * v)\n    e = np.linalg.norm(e_vec)\n\n    inc = np.arccos(np.clip(h_vec[2] / h_mag, -1.0, 1.0))\n\n    k_hat = np.array([0.0, 0.0, 1.0])\n    n_vec = np.cross(k_hat, h_vec)\n    n_mag = np.linalg.norm(n_vec)\n\n    if n_mag &gt; 1e-9:\n        raan = np.arccos(np.clip(n_vec[0] / n_mag, -1.0, 1.0))\n        if n_vec[1] &lt; 0:\n            raan = 2.0 * np.pi - raan\n    else:\n        raan = 0.0\n\n    if n_mag &gt; 1e-9 and e &gt; 1e-6:\n        argp = np.arccos(np.clip(np.dot(n_vec, e_vec) / (n_mag * e), -1.0, 1.0))\n        if e_vec[2] &lt; 0:\n            argp = 2.0 * np.pi - argp\n    else:\n        argp = 0.0\n\n    return {\n        \"a\": a,\n        \"e\": e,\n        \"inc_deg\": np.degrees(inc),\n        \"raan_deg\": np.degrees(raan),\n        \"argp_deg\": np.degrees(argp)\n    }\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_lunar_gravity","title":"accel_lunar_gravity","text":"<pre><code>accel_lunar_gravity(r: ndarray, t: float) -&gt; ndarray\n</code></pre> <p>Compute third-body lunar gravitational perturbation.</p> <p>Accounts for both direct and indirect inertial acceleration:</p> \\[ \\mathbf{a}_{3\\text{rd}} = \\mu_{\\text{moon}} \\left( \\frac{\\mathbf{r}_{\\text{moon}} - \\mathbf{r}}{\\|\\mathbf{r}_{\\text{moon}} - \\mathbf{r}\\|^3} - \\frac{\\mathbf{r}_{\\text{moon}}}{\\|\\mathbf{r}_{\\text{moon}}\\|^3} \\right) \\] <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Spacecraft position vector \\(\\mathbf{r}\\) in the ECI frame [\\(\\text{m}\\)].</p> required <code>t</code> <code>float</code> <p>Elapsed simulation epoch time \\(t\\) [\\(\\text{s}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Third-body lunar acceleration vector \\(\\mathbf{a}_{3\\text{rd}}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_lunar_gravity(r: np.ndarray, t: float) -&gt; np.ndarray:\n    r\"\"\"Compute third-body lunar gravitational perturbation.\n\n    Accounts for both direct and indirect inertial acceleration:\n\n    $$\n    \\mathbf{a}_{3\\text{rd}} = \\mu_{\\text{moon}} \\left( \\frac{\\mathbf{r}_{\\text{moon}} - \\mathbf{r}}{\\|\\mathbf{r}_{\\text{moon}} - \\mathbf{r}\\|^3} - \\frac{\\mathbf{r}_{\\text{moon}}}{\\|\\mathbf{r}_{\\text{moon}}\\|^3} \\right)\n    $$\n\n    Args:\n        r: Spacecraft position vector $\\mathbf{r}$ in the ECI frame [$\\text{m}$].\n        t: Elapsed simulation epoch time $t$ [$\\text{s}$].\n\n    Returns:\n        np.ndarray: Third-body lunar acceleration vector $\\mathbf{a}_{3\\text{rd}}$ [$\\text{m/s}^2$].\n    \"\"\"\n    r_moon = np.array([\n        R_MOON_ORBIT * np.cos(OMEGA_MOON * t),\n        R_MOON_ORBIT * np.sin(OMEGA_MOON * t),\n        0.0\n    ])\n    r_rel = r_moon - r\n    return G_MOON * (r_rel / (np.linalg.norm(r_rel) ** 3) - r_moon / (R_MOON_ORBIT ** 3))\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_atmospheric_drag","title":"accel_atmospheric_drag","text":"<pre><code>accel_atmospheric_drag(\n    r: ndarray,\n    v: ndarray,\n    cd: float,\n    area: float,\n    mass: float,\n) -&gt; ndarray\n</code></pre> <p>Compute atmospheric drag acceleration with exponential density falloff.</p> <p>Accounts for Earth diurnal rotation in calculating relative wind velocity:</p> \\[ \\rho(h) = \\rho_0 \\exp\\left(-\\frac{h}{H}\\right), \\quad \\mathbf{v}_{\\text{rel}} = \\mathbf{v} - \\mathbf{\\omega}_{\\oplus} \\times \\mathbf{r} \\] \\[ \\mathbf{a}_{\\text{drag}} = -\\frac{1}{2} \\rho \\left( \\frac{C_d A}{m} \\right) \\|\\mathbf{v}_{\\text{rel}}\\| \\mathbf{v}_{\\text{rel}} \\] <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Spacecraft position vector \\(\\mathbf{r}\\) in the ECI frame [\\(\\text{m}\\)].</p> required <code>v</code> <code>ndarray</code> <p>Spacecraft inertial velocity vector \\(\\mathbf{v}\\) in the ECI frame [\\(\\text{m/s}\\)].</p> required <code>cd</code> <code>float</code> <p>Aerodynamic drag coefficient \\(C_d\\) [dimensionless].</p> required <code>area</code> <code>float</code> <p>Frontal aerodynamic reference cross-sectional area \\(A\\) [\\(\\text{m}^2\\)].</p> required <code>mass</code> <code>float</code> <p>Instantaneous spacecraft mass \\(m\\) [\\(\\text{kg}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Aerodynamic drag acceleration vector \\(\\mathbf{a}_{\\text{drag}}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_atmospheric_drag(\n    r: np.ndarray,\n    v: np.ndarray,\n    cd: float,\n    area: float,\n    mass: float\n) -&gt; np.ndarray:\n    r\"\"\"Compute atmospheric drag acceleration with exponential density falloff.\n\n    Accounts for Earth diurnal rotation in calculating relative wind velocity:\n\n    $$\n    \\rho(h) = \\rho_0 \\exp\\left(-\\frac{h}{H}\\right), \\quad\n    \\mathbf{v}_{\\text{rel}} = \\mathbf{v} - \\mathbf{\\omega}_{\\oplus} \\times \\mathbf{r}\n    $$\n\n    $$\n    \\mathbf{a}_{\\text{drag}} = -\\frac{1}{2} \\rho \\left( \\frac{C_d A}{m} \\right) \\|\\mathbf{v}_{\\text{rel}}\\| \\mathbf{v}_{\\text{rel}}\n    $$\n\n    Args:\n        r: Spacecraft position vector $\\mathbf{r}$ in the ECI frame [$\\text{m}$].\n        v: Spacecraft inertial velocity vector $\\mathbf{v}$ in the ECI frame [$\\text{m/s}$].\n        cd: Aerodynamic drag coefficient $C_d$ [dimensionless].\n        area: Frontal aerodynamic reference cross-sectional area $A$ [$\\text{m}^2$].\n        mass: Instantaneous spacecraft mass $m$ [$\\text{kg}$].\n\n    Returns:\n        np.ndarray: Aerodynamic drag acceleration vector $\\mathbf{a}_{\\text{drag}}$ [$\\text{m/s}^2$].\n    \"\"\"\n    altitude = np.linalg.norm(r) - R_EARTH\n    if altitude &lt; 0:\n        return np.array([0.0, 0.0, 0.0])\n\n    rho = RHO_0 * np.exp(-altitude / SCALE_HEIGHT)\n    omega_vec = np.array([0.0, 0.0, OMEGA_EARTH])\n    v_rel = v - np.cross(omega_vec, r)\n    v_rel_mag = np.linalg.norm(v_rel)\n\n    return -0.5 * rho * (cd * area / mass) * v_rel_mag * v_rel\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_solar_radiation_pressure","title":"accel_solar_radiation_pressure","text":"<pre><code>accel_solar_radiation_pressure(\n    r: ndarray, cr: float, area: float, mass: float\n) -&gt; ndarray\n</code></pre> <p>Compute cannonball Solar Radiation Pressure (SRP) with cylindrical Earth eclipse shadow.</p> \\[ \\mathbf{a}_{\\text{srp}} = \\nu P_{\\text{sun}} C_r \\left(\\frac{A}{m}\\right) \\frac{\\mathbf{r}_{\\text{sc}} - \\mathbf{r}_{\\odot}}{\\|\\mathbf{r}_{\\text{sc}} - \\mathbf{r}_{\\odot}\\|} \\] <p>where \\(\\nu = 0\\) inside the cylindrical umbra (\\(x &lt; 0\\) and \\(\\sqrt{y^2 + z^2} &lt; R_{\\oplus}\\)), and \\(\\nu = 1\\) in direct sunlight.</p> <p>Parameters:</p> Name Type Description Default <code>r</code> <code>ndarray</code> <p>Spacecraft position vector \\(\\mathbf{r}\\) in the ECI frame [\\(\\text{m}\\)].</p> required <code>cr</code> <code>float</code> <p>Radiation pressure reflectivity coefficient \\(C_r\\) [dimensionless].</p> required <code>area</code> <code>float</code> <p>Solar exposed cross-sectional area \\(A\\) [\\(\\text{m}^2\\)].</p> required <code>mass</code> <code>float</code> <p>Instantaneous spacecraft mass \\(m\\) [\\(\\text{kg}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Solar radiation pressure acceleration vector \\(\\mathbf{a}_{\\text{srp}}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_solar_radiation_pressure(\n    r: np.ndarray,\n    cr: float,\n    area: float,\n    mass: float\n) -&gt; np.ndarray:\n    r\"\"\"Compute cannonball Solar Radiation Pressure (SRP) with cylindrical Earth eclipse shadow.\n\n    $$\n    \\mathbf{a}_{\\text{srp}} = \\nu P_{\\text{sun}} C_r \\left(\\frac{A}{m}\\right) \\frac{\\mathbf{r}_{\\text{sc}} - \\mathbf{r}_{\\odot}}{\\|\\mathbf{r}_{\\text{sc}} - \\mathbf{r}_{\\odot}\\|}\n    $$\n\n    where $\\nu = 0$ inside the cylindrical umbra ($x &lt; 0$ and $\\sqrt{y^2 + z^2} &lt; R_{\\oplus}$), and $\\nu = 1$ in direct sunlight.\n\n    Args:\n        r: Spacecraft position vector $\\mathbf{r}$ in the ECI frame [$\\text{m}$].\n        cr: Radiation pressure reflectivity coefficient $C_r$ [dimensionless].\n        area: Solar exposed cross-sectional area $A$ [$\\text{m}^2$].\n        mass: Instantaneous spacecraft mass $m$ [$\\text{kg}$].\n\n    Returns:\n        np.ndarray: Solar radiation pressure acceleration vector $\\mathbf{a}_{\\text{srp}}$ [$\\text{m/s}^2$].\n    \"\"\"\n    r_sun = np.array([AU, 0.0, 0.0])\n    r_sc_sun = r - r_sun\n    sun_dir = r_sc_sun / np.linalg.norm(r_sc_sun)\n\n    if r[0] &lt; 0 and np.linalg.norm(r[1:3]) &lt; R_EARTH:\n        return np.array([0.0, 0.0, 0.0])\n\n    return P_SUN_1AU * cr * (area / mass) * sun_dir\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_fixed_thrust","title":"accel_fixed_thrust","text":"<pre><code>accel_fixed_thrust(\n    t: float,\n    start_t: float,\n    duration: float,\n    thrust_vec: ndarray,\n    mass: float,\n) -&gt; ndarray\n</code></pre> <p>Compute constant directional thrust acceleration over an active window.</p> \\[ \\mathbf{a}_{\\text{thrust}} = \\begin{cases} \\frac{\\mathbf{F}_{\\text{thrust}}}{m}, &amp; t_{\\text{start}} \\le t \\le t_{\\text{start}} + \\Delta t \\\\ \\mathbf{0}, &amp; \\text{otherwise} \\end{cases} \\] <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Current simulation epoch time \\(t\\) [\\(\\text{s}\\)].</p> required <code>start_t</code> <code>float</code> <p>Engine ignition epoch \\(t_{\\text{start}}\\) [\\(\\text{s}\\)].</p> required <code>duration</code> <code>float</code> <p>Total engine burn duration \\(\\Delta t\\) [\\(\\text{s}\\)].</p> required <code>thrust_vec</code> <code>ndarray</code> <p>Applied thrust vector \\(\\mathbf{F}_{\\text{thrust}}\\) [\\(\\text{N}\\)].</p> required <code>mass</code> <code>float</code> <p>Instantaneous spacecraft mass \\(m\\) [\\(\\text{kg}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Thrust acceleration vector \\(\\mathbf{a}_{\\text{thrust}}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_fixed_thrust(\n    t: float,\n    start_t: float,\n    duration: float,\n    thrust_vec: np.ndarray,\n    mass: float\n) -&gt; np.ndarray:\n    r\"\"\"Compute constant directional thrust acceleration over an active window.\n\n    $$\n    \\mathbf{a}_{\\text{thrust}} =\n    \\begin{cases}\n    \\frac{\\mathbf{F}_{\\text{thrust}}}{m}, &amp; t_{\\text{start}} \\le t \\le t_{\\text{start}} + \\Delta t \\\\\n    \\mathbf{0}, &amp; \\text{otherwise}\n    \\end{cases}\n    $$\n\n    Args:\n        t: Current simulation epoch time $t$ [$\\text{s}$].\n        start_t: Engine ignition epoch $t_{\\text{start}}$ [$\\text{s}$].\n        duration: Total engine burn duration $\\Delta t$ [$\\text{s}$].\n        thrust_vec: Applied thrust vector $\\mathbf{F}_{\\text{thrust}}$ [$\\text{N}$].\n        mass: Instantaneous spacecraft mass $m$ [$\\text{kg}$].\n\n    Returns:\n        np.ndarray: Thrust acceleration vector $\\mathbf{a}_{\\text{thrust}}$ [$\\text{m/s}^2$].\n    \"\"\"\n    if start_t &lt;= t &lt;= (start_t + duration):\n        return thrust_vec / mass\n    return np.array([0.0, 0.0, 0.0])\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.accel_electric_prograde","title":"accel_electric_prograde","text":"<pre><code>accel_electric_prograde(\n    v: ndarray, thrust_mag: float, mass: float\n) -&gt; ndarray\n</code></pre> <p>Compute continuous low-thrust propulsion aligned with the velocity vector.</p> \\[ \\mathbf{a}_{\\text{low-thrust}} = \\frac{T}{m} \\hat{\\mathbf{v}} = \\frac{T}{m} \\left( \\frac{\\mathbf{v}}{\\|\\mathbf{v}\\|} \\right) \\] <p>Parameters:</p> Name Type Description Default <code>v</code> <code>ndarray</code> <p>Spacecraft inertial velocity vector \\(\\mathbf{v}\\) in the ECI frame [\\(\\text{m/s}\\)].</p> required <code>thrust_mag</code> <code>float</code> <p>Continuous thrust magnitude \\(T\\) [\\(\\text{N}\\)].</p> required <code>mass</code> <code>float</code> <p>Instantaneous spacecraft mass \\(m\\) [\\(\\text{kg}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Prograde low-thrust acceleration vector \\(\\mathbf{a}_{\\text{low-thrust}}\\) [\\(\\text{m/s}^2\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def accel_electric_prograde(\n    v: np.ndarray,\n    thrust_mag: float,\n    mass: float\n) -&gt; np.ndarray:\n    r\"\"\"Compute continuous low-thrust propulsion aligned with the velocity vector.\n\n    $$\n    \\mathbf{a}_{\\text{low-thrust}} = \\frac{T}{m} \\hat{\\mathbf{v}} = \\frac{T}{m} \\left( \\frac{\\mathbf{v}}{\\|\\mathbf{v}\\|} \\right)\n    $$\n\n    Args:\n        v: Spacecraft inertial velocity vector $\\mathbf{v}$ in the ECI frame [$\\text{m/s}$].\n        thrust_mag: Continuous thrust magnitude $T$ [$\\text{N}$].\n        mass: Instantaneous spacecraft mass $m$ [$\\text{kg}$].\n\n    Returns:\n        np.ndarray: Prograde low-thrust acceleration vector $\\mathbf{a}_{\\text{low-thrust}}$ [$\\text{m/s}^2$].\n    \"\"\"\n    v_mag = np.linalg.norm(v)\n    if v_mag == 0.0:\n        return np.array([0.0, 0.0, 0.0])\n    return (thrust_mag / mass) * (v / v_mag)\n</code></pre>"},{"location":"reference/dynamics_propagation/forces/#core.forces.eci_to_lvlh","title":"eci_to_lvlh","text":"<pre><code>eci_to_lvlh(\n    r_chief: ndarray, v_chief: ndarray, r_deputy: ndarray\n) -&gt; ndarray\n</code></pre> <p>Transform deputy position relative to chief from ECI to Local-Vertical/Local-Horizontal (LVLH).</p> <p>Constructs the standard orbital reference triad:</p> \\[ \\hat{\\mathbf{e}}_r = \\frac{\\mathbf{r}_c}{\\|\\mathbf{r}_c\\|}, \\quad \\hat{\\mathbf{e}}_h = \\frac{\\mathbf{r}_c \\times \\mathbf{v}_c}{\\|\\mathbf{r}_c \\times \\mathbf{v}_c\\|}, \\quad \\hat{\\mathbf{e}}_\\theta = \\hat{\\mathbf{e}}_h \\times \\hat{\\mathbf{e}}_r \\] <p>and maps relative displacement \\(\\delta \\mathbf{r}_{\\text{ECI}} = \\mathbf{r}_d - \\mathbf{r}_c\\):</p> \\[ \\delta \\mathbf{r}_{\\text{LVLH}} = \\begin{bmatrix} \\hat{\\mathbf{e}}_r^T \\\\ \\hat{\\mathbf{e}}_\\theta^T \\\\ \\hat{\\mathbf{e}}_h^T \\end{bmatrix} \\delta \\mathbf{r}_{\\text{ECI}} = \\begin{bmatrix} x_{\\text{radial}} \\\\ y_{\\text{along-track}} \\\\ z_{\\text{cross-track}} \\end{bmatrix} \\] <p>Parameters:</p> Name Type Description Default <code>r_chief</code> <code>ndarray</code> <p>Chief position vector \\(\\mathbf{r}_c\\) in the ECI frame [\\(\\text{m}\\)].</p> required <code>v_chief</code> <code>ndarray</code> <p>Chief velocity vector \\(\\mathbf{v}_c\\) in the ECI frame [\\(\\text{m/s}\\)].</p> required <code>r_deputy</code> <code>ndarray</code> <p>Deputy position vector \\(\\mathbf{r}_d\\) in the ECI frame [\\(\\text{m}\\)].</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Relative displacement \\(\\delta \\mathbf{r}_{\\text{LVLH}} = [x_{\\text{radial}}, y_{\\text{along-track}}, z_{\\text{cross-track}}]^T\\) [\\(\\text{m}\\)].</p> Source code in <code>core/forces.py</code> <pre><code>def eci_to_lvlh(\n    r_chief: np.ndarray,\n    v_chief: np.ndarray,\n    r_deputy: np.ndarray\n) -&gt; np.ndarray:\n    r\"\"\"Transform deputy position relative to chief from ECI to Local-Vertical/Local-Horizontal (LVLH).\n\n    Constructs the standard orbital reference triad:\n\n    $$\n    \\hat{\\mathbf{e}}_r = \\frac{\\mathbf{r}_c}{\\|\\mathbf{r}_c\\|}, \\quad\n    \\hat{\\mathbf{e}}_h = \\frac{\\mathbf{r}_c \\times \\mathbf{v}_c}{\\|\\mathbf{r}_c \\times \\mathbf{v}_c\\|}, \\quad\n    \\hat{\\mathbf{e}}_\\theta = \\hat{\\mathbf{e}}_h \\times \\hat{\\mathbf{e}}_r\n    $$\n\n    and maps relative displacement $\\delta \\mathbf{r}_{\\text{ECI}} = \\mathbf{r}_d - \\mathbf{r}_c$:\n\n    $$\n    \\delta \\mathbf{r}_{\\text{LVLH}} =\n    \\begin{bmatrix}\n    \\hat{\\mathbf{e}}_r^T \\\\\n    \\hat{\\mathbf{e}}_\\theta^T \\\\\n    \\hat{\\mathbf{e}}_h^T\n    \\end{bmatrix}\n    \\delta \\mathbf{r}_{\\text{ECI}} =\n    \\begin{bmatrix}\n    x_{\\text{radial}} \\\\\n    y_{\\text{along-track}} \\\\\n    z_{\\text{cross-track}}\n    \\end{bmatrix}\n    $$\n\n    Args:\n        r_chief: Chief position vector $\\mathbf{r}_c$ in the ECI frame [$\\text{m}$].\n        v_chief: Chief velocity vector $\\mathbf{v}_c$ in the ECI frame [$\\text{m/s}$].\n        r_deputy: Deputy position vector $\\mathbf{r}_d$ in the ECI frame [$\\text{m}$].\n\n    Returns:\n        np.ndarray: Relative displacement $\\delta \\mathbf{r}_{\\text{LVLH}} = [x_{\\text{radial}}, y_{\\text{along-track}}, z_{\\text{cross-track}}]^T$ [$\\text{m}$].\n    \"\"\"\n    r_c_mag = np.linalg.norm(r_chief)\n    e_r = r_chief / r_c_mag\n\n    h_vec = np.cross(r_chief, v_chief)\n    e_h = h_vec / np.linalg.norm(h_vec)\n\n    e_theta = np.cross(e_h, e_r)\n\n    r_eci_to_lvlh = np.vstack([e_r, e_theta, e_h])\n    delta_r_eci = r_deputy - r_chief\n\n    return r_eci_to_lvlh @ delta_r_eci\n</code></pre>"},{"location":"reference/dynamics_propagation/integrators/","title":"integrators","text":""},{"location":"reference/dynamics_propagation/integrators/#core.integrators","title":"core.integrators","text":"<p>Numerical Integration Routines for Spacecraft Trajectory Propagation.</p> <p>Provides: - <code>rk4_step</code>: Standard fixed-step 4th-order Runge-Kutta single step. - <code>rk4</code>: Fixed-step Runge-Kutta numerical propagator. - <code>rk45_adaptive</code>: Adaptive step-size Dormand-Prince (DOPRI5) integrator   with embedded local truncation error (LTE) control and FSAL optimization.</p>"},{"location":"reference/dynamics_propagation/integrators/#core.integrators.rk4_step","title":"rk4_step","text":"<pre><code>rk4_step(\n    derivs_func: Callable[[float, ndarray], ndarray],\n    t: float,\n    y: ndarray,\n    dt: float,\n) -&gt; ndarray\n</code></pre> <p>Compute a single classical 4th-order Runge-Kutta step.</p> <p>Parameters:</p> Name Type Description Default <code>derivs_func</code> <code>Callable[[float, ndarray], ndarray]</code> <p>Right-hand side derivative function \\(\\mathbf{f}(t, \\mathbf{y})\\).</p> required <code>t</code> <code>float</code> <p>Current epoch time in seconds.</p> required <code>y</code> <code>ndarray</code> <p>Current state vector \\(\\mathbf{y}(t)\\) of shape <code>(n,)</code>.</p> required <code>dt</code> <code>float</code> <p>Time step size \\(\\Delta t\\) in seconds.</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: State vector advanced to \\(t + \\Delta t\\) of shape <code>(n,)</code>.</p> Source code in <code>core/integrators.py</code> <pre><code>def rk4_step(\n    derivs_func: Callable[[float, np.ndarray], np.ndarray],\n    t: float,\n    y: np.ndarray,\n    dt: float\n) -&gt; np.ndarray:\n    r\"\"\"Compute a single classical 4th-order Runge-Kutta step.\n\n    Args:\n        derivs_func: Right-hand side derivative function $\\mathbf{f}(t, \\mathbf{y})$.\n        t: Current epoch time in seconds.\n        y: Current state vector $\\mathbf{y}(t)$ of shape `(n,)`.\n        dt: Time step size $\\Delta t$ in seconds.\n\n    Returns:\n        np.ndarray: State vector advanced to $t + \\Delta t$ of shape `(n,)`.\n    \"\"\"\n    k1 = derivs_func(t, y)\n    k2 = derivs_func(t + 0.5 * dt, y + 0.5 * dt * k1)\n    k3 = derivs_func(t + 0.5 * dt, y + 0.5 * dt * k2)\n    k4 = derivs_func(t + dt, y + dt * k3)\n    return y + (dt / 6.0) * (k1 + 2.0 * k2 + 2.0 * k3 + k4)\n</code></pre>"},{"location":"reference/dynamics_propagation/integrators/#core.integrators.rk4","title":"rk4","text":"<pre><code>rk4(\n    derivs_func: Callable[[float, ndarray], ndarray],\n    t_span: Tuple[float, float],\n    y0: ndarray,\n    dt: float,\n) -&gt; Tuple[ndarray, ndarray]\n</code></pre> <p>Integrate an ODE initial-value problem using fixed-step RK4.</p> <p>Parameters:</p> Name Type Description Default <code>derivs_func</code> <code>Callable[[float, ndarray], ndarray]</code> <p>Right-hand side derivative function \\(\\mathbf{f}(t, \\mathbf{y})\\).</p> required <code>t_span</code> <code>Tuple[float, float]</code> <p>Integration bounds \\((t_{\\text{start}}, t_{\\text{final}})\\) in seconds.</p> required <code>y0</code> <code>ndarray</code> <p>Initial state vector \\(\\mathbf{y}_0\\) of shape <code>(n,)</code>.</p> required <code>dt</code> <code>float</code> <p>Fixed time step size \\(\\Delta t\\) in seconds.</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>Tuple[np.ndarray, np.ndarray]: A tuple <code>(t_eval, y_eval)</code> containing: - t_eval (<code>np.ndarray</code>): Discrete time points of shape <code>(N,)</code> [\\(\\text{s}\\)]. - y_eval (<code>np.ndarray</code>): State vector histories of shape <code>(N, n)</code>.</p> Source code in <code>core/integrators.py</code> <pre><code>def rk4(\n    derivs_func: Callable[[float, np.ndarray], np.ndarray],\n    t_span: Tuple[float, float],\n    y0: np.ndarray,\n    dt: float\n) -&gt; Tuple[np.ndarray, np.ndarray]:\n    r\"\"\"Integrate an ODE initial-value problem using fixed-step RK4.\n\n    Args:\n        derivs_func: Right-hand side derivative function $\\mathbf{f}(t, \\mathbf{y})$.\n        t_span: Integration bounds $(t_{\\text{start}}, t_{\\text{final}})$ in seconds.\n        y0: Initial state vector $\\mathbf{y}_0$ of shape `(n,)`.\n        dt: Fixed time step size $\\Delta t$ in seconds.\n\n    Returns:\n        Tuple[np.ndarray, np.ndarray]: A tuple `(t_eval, y_eval)` containing:\n            - **t_eval** (`np.ndarray`): Discrete time points of shape `(N,)` [$\\text{s}$].\n            - **y_eval** (`np.ndarray`): State vector histories of shape `(N, n)`.\n    \"\"\"\n    t_start, t_final = t_span\n    t_eval = np.arange(t_start, t_final + dt, dt)\n    num_steps = len(t_eval)\n    state_dim = len(y0)\n\n    y_eval = np.zeros((num_steps, state_dim), dtype=np.float64)\n    y_eval[0] = np.asarray(y0, dtype=np.float64)\n\n    for i in range(num_steps - 1):\n        y_eval[i + 1] = rk4_step(derivs_func, t_eval[i], y_eval[i], dt)\n\n    return t_eval, y_eval\n</code></pre>"},{"location":"reference/dynamics_propagation/integrators/#core.integrators.rk45_adaptive","title":"rk45_adaptive","text":"<pre><code>rk45_adaptive(\n    derivs_func: Callable[[float, ndarray], ndarray],\n    t_span: Tuple[float, float],\n    y0: ndarray,\n    rtol: float = 1e-08,\n    atol: float = 1e-10,\n    h_init: Optional[float] = None,\n    h_min: float = 1e-06,\n    h_max: float = 86400.0,\n    max_steps: int = 1000000,\n) -&gt; Tuple[ndarray, ndarray]\n</code></pre> <p>Integrate an ODE initial-value problem using adaptive step-size Dormand-Prince 5(4).</p> Features <ul> <li>Embedded 5th-order solution with 4th-order local truncation error estimation.</li> <li>FSAL (First-Same-As-Last) evaluation optimization.</li> <li>Proportional-Integral step-size scaling with safety factors.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>derivs_func</code> <code>Callable[[float, ndarray], ndarray]</code> <p>Right-hand side derivative function \\(\\mathbf{f}(t, \\mathbf{y})\\).</p> required <code>t_span</code> <code>Tuple[float, float]</code> <p>Integration span \\((t_{\\text{start}}, t_{\\text{final}})\\) in seconds.</p> required <code>y0</code> <code>ndarray</code> <p>Initial state vector \\(\\mathbf{y}_0\\) of shape <code>(n,)</code>.</p> required <code>rtol</code> <code>float</code> <p>Relative error tolerance. Defaults to <code>1e-8</code>.</p> <code>1e-08</code> <code>atol</code> <code>float</code> <p>Absolute error tolerance. Defaults to <code>1e-10</code>.</p> <code>1e-10</code> <code>h_init</code> <code>Optional[float]</code> <p>Initial candidate step size in seconds. If <code>None</code>, estimated automatically.</p> <code>None</code> <code>h_min</code> <code>float</code> <p>Minimum allowable time step in seconds. Defaults to <code>1e-6</code>.</p> <code>1e-06</code> <code>h_max</code> <code>float</code> <p>Maximum allowable time step in seconds. Defaults to <code>86400.0</code>.</p> <code>86400.0</code> <code>max_steps</code> <code>int</code> <p>Maximum number of integration steps permitted before aborting. Defaults to <code>1_000_000</code>.</p> <code>1000000</code> <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>Tuple[np.ndarray, np.ndarray]: A tuple <code>(t_history, y_history)</code> containing: - t_history (<code>np.ndarray</code>): Dynamically accepted time points of shape <code>(M,)</code> [\\(\\text{s}\\)]. - y_history (<code>np.ndarray</code>): Integrated state vectors of shape <code>(M, n)</code>.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If integration exceeds <code>max_steps</code> without reaching \\(t_{\\text{final}}\\).</p> Source code in <code>core/integrators.py</code> <pre><code>def rk45_adaptive(\n    derivs_func: Callable[[float, np.ndarray], np.ndarray],\n    t_span: Tuple[float, float],\n    y0: np.ndarray,\n    rtol: float = 1e-8,\n    atol: float = 1e-10,\n    h_init: Optional[float] = None,\n    h_min: float = 1e-6,\n    h_max: float = 86400.0,\n    max_steps: int = 1_000_000\n) -&gt; Tuple[np.ndarray, np.ndarray]:\n    r\"\"\"Integrate an ODE initial-value problem using adaptive step-size Dormand-Prince 5(4).\n\n    Features:\n        - Embedded 5th-order solution with 4th-order local truncation error estimation.\n        - FSAL (First-Same-As-Last) evaluation optimization.\n        - Proportional-Integral step-size scaling with safety factors.\n\n    Args:\n        derivs_func: Right-hand side derivative function $\\mathbf{f}(t, \\mathbf{y})$.\n        t_span: Integration span $(t_{\\text{start}}, t_{\\text{final}})$ in seconds.\n        y0: Initial state vector $\\mathbf{y}_0$ of shape `(n,)`.\n        rtol: Relative error tolerance. Defaults to `1e-8`.\n        atol: Absolute error tolerance. Defaults to `1e-10`.\n        h_init: Initial candidate step size in seconds. If `None`, estimated automatically.\n        h_min: Minimum allowable time step in seconds. Defaults to `1e-6`.\n        h_max: Maximum allowable time step in seconds. Defaults to `86400.0`.\n        max_steps: Maximum number of integration steps permitted before aborting. Defaults to `1_000_000`.\n\n    Returns:\n        Tuple[np.ndarray, np.ndarray]: A tuple `(t_history, y_history)` containing:\n            - **t_history** (`np.ndarray`): Dynamically accepted time points of shape `(M,)` [$\\text{s}$].\n            - **y_history** (`np.ndarray`): Integrated state vectors of shape `(M, n)`.\n\n    Raises:\n        RuntimeError: If integration exceeds `max_steps` without reaching $t_{\\text{final}}$.\n    \"\"\"\n    t_start, t_final = float(t_span[0]), float(t_span[1])\n    direction = 1.0 if t_final &gt;= t_start else -1.0\n\n    t = t_start\n    y = np.asarray(y0, dtype=np.float64).copy()\n\n    k1 = np.asarray(derivs_func(t, y), dtype=np.float64)\n\n    if h_init is None:\n        d0 = np.linalg.norm(y / (atol + np.abs(y) * rtol))\n        d1 = np.linalg.norm(k1 / (atol + np.abs(y) * rtol))\n        if d0 &lt; 1e-5 or d1 &lt; 1e-5:\n            h = 1e-4\n        else:\n            h = 0.01 * (d0 / d1)\n        h = max(h_min, min(h, h_max, abs(t_final - t_start)))\n    else:\n        h = float(h_init)\n\n    t_list = [t]\n    y_list = [y.copy()]\n\n    safety = 0.90\n    fac_min = 0.2\n    fac_max = 5.0\n    step_count = 0\n\n    while (direction * (t_final - t)) &gt; 1e-12:\n        if step_count &gt; max_steps:\n            raise RuntimeError(\n                f\"Adaptive RK45 exceeded maximum allowed steps ({max_steps}). \"\n                f\"Current time: {t:.4f} s, Target: {t_final:.4f} s.\"\n            )\n\n        if (direction * (t + direction * h)) &gt; (direction * t_final):\n            h_step = abs(t_final - t)\n        else:\n            h_step = h\n\n        h_signed = direction * h_step\n\n        k2 = derivs_func(t + C2 * h_signed, y + h_signed * (A21 * k1))\n        k3 = derivs_func(t + C3 * h_signed, y + h_signed * (A31 * k1 + A32 * k2))\n        k4 = derivs_func(t + C4 * h_signed, y + h_signed * (A41 * k1 + A42 * k2 + A43 * k3))\n        k5 = derivs_func(t + C5 * h_signed, y + h_signed * (A51 * k1 + A52 * k2 + A53 * k3 + A54 * k4))\n        k6 = derivs_func(t + C6 * h_signed, y + h_signed * (A61 * k1 + A62 * k2 + A63 * k3 + A64 * k4 + A65 * k5))\n\n        y_next = y + h_signed * (A71 * k1 + A73 * k3 + A74 * k4 + A75 * k5 + A76 * k6)\n        k7 = derivs_func(t + h_signed, y_next)\n\n        error_vec = h_signed * (E1 * k1 + E3 * k3 + E4 * k4 + E5 * k5 + E6 * k6 + E7 * k7)\n        scale = atol + np.maximum(np.abs(y), np.abs(y_next)) * rtol\n        error_norm = np.sqrt(np.mean((error_vec / scale) ** 2))\n\n        if error_norm &lt;= 1.0:\n            t += h_signed\n            y = y_next\n            step_count += 1\n\n            t_list.append(t)\n            y_list.append(y.copy())\n\n            k1 = k7\n\n            if error_norm == 0.0:\n                scale_factor = fac_max\n            else:\n                scale_factor = min(fac_max, max(fac_min, safety * (error_norm ** -0.2)))\n            h = min(h_max, max(h_min, h_step * scale_factor))\n        else:\n            scale_factor = max(fac_min, safety * (error_norm ** -0.2))\n            h = max(h_min, h_step * scale_factor)\n            if h &lt;= h_min and h_step &lt;= h_min:\n                t += h_signed\n                y = y_next\n                step_count += 1\n                t_list.append(t)\n                y_list.append(y.copy())\n                k1 = derivs_func(t, y)\n\n    return np.array(t_list, dtype=np.float64), np.array(y_list, dtype=np.float64)\n</code></pre>"},{"location":"reference/dynamics_propagation/propagator/","title":"propagator","text":""},{"location":"reference/dynamics_propagation/propagator/#core.propagator","title":"core.propagator","text":"<p>Unified Spacecraft Numerical Propagation Engine.</p> <p>Supports 6-DOF \\((\\mathbf{r}, \\mathbf{v})\\) and 7-DOF \\((\\mathbf{r}, \\mathbf{v}, m)\\) orbital trajectory simulation under primary two-body gravity, zonal harmonics (\\(J_2\\)\u2013\\(J_4\\)), third-body lunar perturbations, atmospheric drag, solar radiation pressure (SRP), and continuous or impulsive thrust.</p> <p>Provides selectable numerical integration backends: - <code>'rk4'</code>: Fixed-step 4th-order Runge-Kutta. - <code>'rk45'</code>: Adaptive step-size Dormand-Prince 5(4) with embedded LTE control.</p>"},{"location":"reference/dynamics_propagation/propagator/#core.propagator.SpacecraftPropagator","title":"SpacecraftPropagator","text":"<pre><code>SpacecraftPropagator(\n    mass: float = 500.0,\n    drag_area: float = 2.0,\n    cd: float = 2.2,\n    srp_area: float = 4.0,\n    cr: float = 1.2,\n    isp: float = 1800.0,\n    use_j2: bool = True,\n    use_j3: bool = False,\n    use_j4: bool = False,\n    use_lunar: bool = False,\n    use_drag: bool = False,\n    use_srp: bool = False,\n    use_thrust: bool = False,\n    mu: float = G_EARTH,\n    r_body: float = R_EARTH,\n    area_drag: Optional[float] = None,\n    area_srp: Optional[float] = None,\n    thrust_mag: float = 0.0,\n    thrust_steering_law: Optional[\n        Callable[[float, ndarray, ndarray, float], ndarray]\n    ] = None,\n    **kwargs,\n)\n</code></pre> <p>Unified orbital propagation engine supporting 6-DOF and 7-DOF dynamics.</p> Source code in <code>core/propagator.py</code> <pre><code>def __init__(\n    self,\n    mass: float = 500.0,\n    drag_area: float = 2.0,\n    cd: float = 2.2,\n    srp_area: float = 4.0,\n    cr: float = 1.2,\n    isp: float = 1800.0,\n    use_j2: bool = True,\n    use_j3: bool = False,\n    use_j4: bool = False,\n    use_lunar: bool = False,\n    use_drag: bool = False,\n    use_srp: bool = False,\n    use_thrust: bool = False,\n    mu: float = G_EARTH,\n    r_body: float = R_EARTH,\n    area_drag: Optional[float] = None,\n    area_srp: Optional[float] = None,\n    thrust_mag: float = 0.0,\n    thrust_steering_law: Optional[Callable[[float, np.ndarray, np.ndarray, float], np.ndarray]] = None,\n    **kwargs,\n):\n    self.mass = float(mass)\n    self.cd = float(cd)\n    self.cr = float(cr)\n    self.isp = float(isp)\n    self.mu = float(mu)\n    self.r_body = float(r_body)\n\n    # Handle alias compatibility between old and new names\n    self.drag_area = float(area_drag if area_drag is not None else drag_area)\n    self.area_drag = self.drag_area\n    self.srp_area = float(area_srp if area_srp is not None else srp_area)\n    self.area_srp = self.srp_area\n\n    # Perturbation flags\n    self.use_j2 = use_j2\n    self.use_j3 = use_j3\n    self.use_j4 = use_j4\n    self.use_lunar = use_lunar\n    self.use_drag = use_drag\n    self.use_srp = use_srp\n    self.use_thrust = use_thrust\n\n    # Thrust modes &amp; steering\n    self.thrust_mode = \"none\"\n    self.thrust_params = {}\n    self.thrust_mag = float(thrust_mag)\n    self.thrust_steering_law = thrust_steering_law\n</code></pre>"},{"location":"reference/dynamics_propagation/propagator/#core.propagator.SpacecraftPropagator.configure_electric_burn","title":"configure_electric_burn","text":"<pre><code>configure_electric_burn(\n    thrust_magnitude: Optional[float] = None,\n    thrust_mag: Optional[float] = None,\n    isp: Optional[float] = None,\n    steering_law: Optional[\n        Callable[[float, ndarray, ndarray, float], ndarray]\n    ] = None,\n) -&gt; SpacecraftPropagator\n</code></pre> <p>Configure continuous prograde low thrust (\\(T\\) in Newtons).</p> Source code in <code>core/propagator.py</code> <pre><code>def configure_electric_burn(\n    self,\n    thrust_magnitude: Optional[float] = None,\n    thrust_mag: Optional[float] = None,\n    isp: Optional[float] = None,\n    steering_law: Optional[Callable[[float, np.ndarray, np.ndarray, float], np.ndarray]] = None,\n) -&gt; \"SpacecraftPropagator\":\n    r\"\"\"Configure continuous prograde low thrust ($T$ in Newtons).\"\"\"\n    mag = thrust_magnitude if thrust_magnitude is not None else (thrust_mag if thrust_mag is not None else 0.0)\n    self.use_thrust = True\n    self.thrust_mode = \"electric_prograde\"\n    self.thrust_mag = float(mag)\n    self.thrust_params = {\"thrust\": float(mag)}\n    if isp is not None:\n        self.isp = float(isp)\n    if steering_law is not None:\n        self.thrust_steering_law = steering_law\n    return self\n</code></pre>"},{"location":"reference/dynamics_propagation/propagator/#core.propagator.SpacecraftPropagator.configure_fixed_burn","title":"configure_fixed_burn","text":"<pre><code>configure_fixed_burn(\n    start_t: float,\n    duration: float,\n    thrust_vec: ndarray,\n    isp: Optional[float] = None,\n) -&gt; SpacecraftPropagator\n</code></pre> <p>Configure an inertial directional burn over \\([t_{\\text{start}}, t_{\\text{start}} + \\Delta t]\\).</p> Source code in <code>core/propagator.py</code> <pre><code>def configure_fixed_burn(\n    self,\n    start_t: float,\n    duration: float,\n    thrust_vec: np.ndarray,\n    isp: Optional[float] = None,\n) -&gt; \"SpacecraftPropagator\":\n    r\"\"\"Configure an inertial directional burn over $[t_{\\text{start}}, t_{\\text{start}} + \\Delta t]$.\"\"\"\n    thrust_arr = np.asarray(thrust_vec, dtype=np.float64)\n    self.use_thrust = True\n    self.thrust_mode = \"fixed\"\n    self.thrust_params = {\n        \"start_t\": float(start_t),\n        \"duration\": float(duration),\n        \"vec\": thrust_arr,\n    }\n    self.thrust_mag = float(np.linalg.norm(thrust_arr))\n    if isp is not None:\n        self.isp = float(isp)\n\n    t_end = float(start_t + duration)\n\n    def _fixed_burn_steering(t: float, r: np.ndarray, v: np.ndarray, mass: float) -&gt; np.ndarray:\n        if start_t &lt;= t &lt;= t_end and mass &gt; 20.0:\n            return thrust_arr / mass\n        return np.zeros(3, dtype=np.float64)\n\n    self.thrust_steering_law = _fixed_burn_steering\n    return self\n</code></pre>"},{"location":"reference/dynamics_propagation/propagator/#core.propagator.SpacecraftPropagator.configure_thrust","title":"configure_thrust","text":"<pre><code>configure_thrust(\n    thrust_mag: Optional[float] = None,\n    thrust_magnitude: Optional[float] = None,\n    isp: Optional[float] = None,\n    steering_law: Optional[\n        Callable[[float, ndarray, ndarray, float], ndarray]\n    ] = None,\n) -&gt; SpacecraftPropagator\n</code></pre> <p>Unified wrapper for low-thrust configuration.</p> Source code in <code>core/propagator.py</code> <pre><code>def configure_thrust(\n    self,\n    thrust_mag: Optional[float] = None,\n    thrust_magnitude: Optional[float] = None,\n    isp: Optional[float] = None,\n    steering_law: Optional[Callable[[float, np.ndarray, np.ndarray, float], np.ndarray]] = None,\n) -&gt; \"SpacecraftPropagator\":\n    r\"\"\"Unified wrapper for low-thrust configuration.\"\"\"\n    return self.configure_electric_burn(\n        thrust_magnitude=thrust_magnitude,\n        thrust_mag=thrust_mag,\n        isp=isp,\n        steering_law=steering_law,\n    )\n</code></pre>"},{"location":"reference/dynamics_propagation/propagator/#core.propagator.SpacecraftPropagator.derivatives","title":"derivatives","text":"<pre><code>derivatives(t: float, state: ndarray) -&gt; ndarray\n</code></pre> <p>Evaluate total state derivative vector \\([d\\mathbf{r}/dt, d\\mathbf{v}/dt (, dm/dt)]^T\\).</p> Source code in <code>core/propagator.py</code> <pre><code>def derivatives(self, t: float, state: np.ndarray) -&gt; np.ndarray:\n    r\"\"\"Evaluate total state derivative vector $[d\\mathbf{r}/dt, d\\mathbf{v}/dt (, dm/dt)]^T$.\"\"\"\n    r = state[0:3]\n    v = state[3:6]\n    current_mass = float(state[6]) if len(state) &gt;= 7 else self.mass\n\n    # Central Newtonian gravity\n    a_total = accel_earth_gravity(r)\n\n    # Superposition of Perturbations\n    if self.use_j2:\n        a_total = a_total + accel_j2_perturbation(r)\n    if self.use_j3:\n        a_total = a_total + accel_j3_perturbation(r)\n    if self.use_j4:\n        a_total = a_total + accel_j4_perturbation(r)\n    if self.use_lunar:\n        a_total = a_total + accel_lunar_gravity(r, t)\n    if self.use_drag:\n        a_total = a_total + accel_atmospheric_drag(r, v, self.cd, self.drag_area, current_mass)\n    if self.use_srp:\n        a_total = a_total + accel_solar_radiation_pressure(r, self.cr, self.srp_area, current_mass)\n\n    # Thrust &amp; Mass Depletion\n    m_dot = 0.0\n    if self.use_thrust or self.thrust_mode != \"none\":\n        if self.thrust_steering_law is not None:\n            a_total = a_total + self.thrust_steering_law(t, r, v, current_mass)\n            if self.thrust_mag &gt; 0.0 and current_mass &gt; 20.0:\n                m_dot = -self.thrust_mag / (G0 * self.isp)\n        elif self.thrust_mode == \"fixed\":\n            a_total = a_total + accel_fixed_thrust(\n                t,\n                self.thrust_params[\"start_t\"],\n                self.thrust_params[\"duration\"],\n                self.thrust_params[\"vec\"],\n                current_mass,\n            )\n            start_t = self.thrust_params[\"start_t\"]\n            t_end = start_t + self.thrust_params[\"duration\"]\n            if start_t &lt;= t &lt;= t_end and current_mass &gt; 20.0:\n                m_dot = -self.thrust_mag / (G0 * self.isp)\n        elif self.thrust_mode == \"electric_prograde\":\n            t_mag = self.thrust_params.get(\"thrust\", self.thrust_mag)\n            if current_mass &gt; 20.0 and t_mag &gt; 0.0:\n                a_total = a_total + accel_electric_prograde(v, t_mag, current_mass)\n                m_dot = -t_mag / (G0 * self.isp)\n\n    if len(state) &gt;= 7:\n        return np.concatenate((v, a_total, [m_dot]))\n    return np.concatenate((v, a_total))\n</code></pre>"},{"location":"reference/dynamics_propagation/propagator/#core.propagator.SpacecraftPropagator.plot_3d","title":"plot_3d  <code>staticmethod</code>","text":"<pre><code>plot_3d(states: ndarray, title: str = 'Trajectory') -&gt; None\n</code></pre> <p>Plot the computed 3D orbit trajectory around a scaled Earth sphere.</p> Source code in <code>core/propagator.py</code> <pre><code>@staticmethod\ndef plot_3d(states: np.ndarray, title: str = \"Trajectory\") -&gt; None:\n    r\"\"\"Plot the computed 3D orbit trajectory around a scaled Earth sphere.\"\"\"\n    states_arr = np.asarray(states)\n    x_km = states_arr[:, 0] / 1000.0\n    y_km = states_arr[:, 1] / 1000.0\n    z_km = states_arr[:, 2] / 1000.0\n\n    fig = plt.figure(figsize=(10.0, 8.0))\n    ax = fig.add_subplot(111, projection=\"3d\")\n\n    # Earth wireframe sphere\n    r_earth_km = R_EARTH / 1000.0\n    u, v = np.mgrid[0 : 2 * np.pi : 30j, 0 : np.pi : 15j]\n    ax.plot_wireframe(\n        r_earth_km * np.cos(u) * np.sin(v),\n        r_earth_km * np.sin(u) * np.sin(v),\n        r_earth_km * np.cos(v),\n        color=\"dodgerblue\",\n        alpha=0.25,\n        label=\"Earth\",\n    )\n\n    # Orbit trajectory\n    ax.plot(x_km, y_km, z_km, color=\"crimson\", linewidth=1.5, label=\"Trajectory\")\n    ax.scatter(x_km[0], y_km[0], z_km[0], color=\"forestgreen\", s=50, label=\"Start\")\n    ax.scatter(x_km[-1], y_km[-1], z_km[-1], color=\"black\", s=50, label=\"End\")\n\n    # Equal aspect ratio scaling\n    max_r = np.array([x_km.max() - x_km.min(), y_km.max() - y_km.min(), z_km.max() - z_km.min()]).max() / 2.0\n    mid_x = (x_km.max() + x_km.min()) * 0.5\n    mid_y = (y_km.max() + y_km.min()) * 0.5\n    mid_z = (z_km.max() + z_km.min()) * 0.5\n    ax.set_xlim(mid_x - max_r, mid_x + max_r)\n    ax.set_ylim(mid_y - max_r, mid_y + max_r)\n    ax.set_zlim(mid_z - max_r, mid_z + max_r)\n\n    ax.set_xlabel(\"ECI X (km)\")\n    ax.set_ylabel(\"ECI Y (km)\")\n    ax.set_zlabel(\"ECI Z (km)\")\n    ax.set_title(title)\n    ax.legend()\n    plt.tight_layout()\n    plt.show()\n</code></pre>"},{"location":"reference/dynamics_propagation/propagator/#core.propagator.SpacecraftPropagator.propagate","title":"propagate","text":"<pre><code>propagate(\n    r0: Optional[ndarray] = None,\n    v0: Optional[ndarray] = None,\n    t_span: float = 86400.0,\n    dt: float = 10.0,\n    track_mass: bool = False,\n    mass0: Optional[float] = None,\n    method: str = \"rk4\",\n    rtol: float = 1e-08,\n    atol: float = 1e-10,\n    h_min: float = 0.0001,\n    h_max: float = 86400.0,\n    state0: Optional[ndarray] = None,\n    **kwargs,\n) -&gt; Tuple[ndarray, ndarray]\n</code></pre> <p>Propagate state vectors across the designated duration.</p> Source code in <code>core/propagator.py</code> <pre><code>def propagate(\n    self,\n    r0: Optional[np.ndarray] = None,\n    v0: Optional[np.ndarray] = None,\n    t_span: float = 86400.0,\n    dt: float = 10.0,\n    track_mass: bool = False,\n    mass0: Optional[float] = None,\n    method: str = \"rk4\",\n    rtol: float = 1e-8,\n    atol: float = 1e-10,\n    h_min: float = 1e-4,\n    h_max: float = 86400.0,\n    state0: Optional[np.ndarray] = None,\n    **kwargs,\n) -&gt; Tuple[np.ndarray, np.ndarray]:\n    r\"\"\"Propagate state vectors across the designated duration.\"\"\"\n    if state0 is not None:\n        state0_arr = np.asarray(state0, dtype=np.float64)\n        r0_arr = state0_arr[0:3]\n        v0_arr = state0_arr[3:6]\n        if len(state0_arr) &gt;= 7:\n            track_mass = True\n            mass0 = float(state0_arr[6])\n    else:\n        if r0 is None or v0 is None:\n            raise ValueError(\"Must provide either (r0, v0) or combined state0.\")\n        r0_arr = np.asarray(r0, dtype=np.float64)\n        v0_arr = np.asarray(v0, dtype=np.float64)\n\n    is_7dof = track_mass or (mass0 is not None)\n    initial_mass = float(mass0) if mass0 is not None else self.mass\n\n    if is_7dof:\n        initial_state = np.concatenate((r0_arr, v0_arr, [initial_mass]))\n    else:\n        initial_state = np.concatenate((r0_arr, v0_arr))\n\n    chosen_method = (method or \"rk4\").strip().lower()\n\n    if chosen_method == \"rk4\":\n        num_steps = int(round(float(t_span) / float(dt))) + 1\n        times = np.linspace(0.0, float(t_span), num_steps)\n        states = np.zeros((num_steps, len(initial_state)), dtype=np.float64)\n        states[0] = initial_state\n\n        actual_dt = times[1] - times[0] if num_steps &gt; 1 else float(dt)\n        for i in range(1, num_steps):\n            states[i] = rk4_step(\n                self.derivatives, times[i - 1], states[i - 1], actual_dt\n            )\n\n        return times, states\n\n    elif chosen_method in (\"rk45\", \"dopri5\"):\n        return rk45_adaptive(\n            derivs_func=self.derivatives,\n            t_span=(0.0, float(t_span)),\n            y0=initial_state,\n            rtol=float(rtol),\n            atol=float(atol),\n            h_init=float(dt),\n            h_min=float(h_min),\n            h_max=float(h_max),\n        )\n    else:\n        raise ValueError(f\"Unsupported numerical integration method '{method}'. Choose 'rk4' or 'rk45'.\")\n</code></pre>"},{"location":"reference/foundations/constants/","title":"constants","text":""},{"location":"reference/foundations/constants/#core.constants","title":"core.constants","text":"<p>Universal physical, planetary, and orbital constants (SI Units).</p>"},{"location":"reference/foundations/constants/#core.constants--fundamental-reference-constants","title":"Fundamental &amp; Reference Constants","text":"Constant Value Units Description <code>G</code> <code>6.67430e-11</code> \\(\\text{m}^3 / (\\text{kg} \\cdot \\text{s}^2)\\) CODATA 2018 Newtonian gravitational constant <code>G0</code> <code>9.80665</code> \\(\\text{m} / \\text{s}^2\\) Standard Earth gravitational acceleration <code>AU</code> <code>149597870700.0</code> \\(\\text{m}\\) Astronomical Unit (IAU 2012 standard)"},{"location":"reference/foundations/constants/#core.constants--planetary-gravitational-parameters-mu-gm","title":"Planetary &amp; Gravitational Parameters (\\(\\mu = GM\\))","text":"Constant Value Units Description <code>G_EARTH</code> <code>3.986004418e14</code> \\(\\text{m}^3 / \\text{s}^2\\) Earth standard gravitational parameter (\\(\\mu_\\oplus\\)) <code>R_EARTH</code> <code>6378137.0</code> \\(\\text{m}\\) WGS-84 Earth equatorial radius (\\(R_\\oplus\\)) <code>OMEGA_EARTH</code> <code>7.2921159e-5</code> \\(\\text{rad} / \\text{s}\\) Earth nominal rotation rate (\\(\\omega_\\oplus\\)) <code>G_MOON</code> <code>4.9048695e12</code> \\(\\text{m}^3 / \\text{s}^2\\) Lunar standard gravitational parameter (\\(\\mu_{\\text{Moon}}\\)) <code>R_MOON</code> <code>1737400.0</code> \\(\\text{m}\\) Mean volumetric radius of the Moon <code>R_MOON_ORBIT</code> <code>3.844e8</code> \\(\\text{m}\\) Earth-Moon mean semi-major axis <code>OMEGA_MOON</code> <code>2.6617e-6</code> \\(\\text{rad} / \\text{s}\\) Lunar mean orbital motion (\\(\\omega_{\\text{Moon}}\\)) <code>G_SUN</code> <code>1.32712440018e20</code> \\(\\text{m}^3 / \\text{s}^2\\) Solar gravitational parameter (\\(\\mu_\\odot\\)) <code>G_MARS</code> <code>4.282837e13</code> \\(\\text{m}^3 / \\text{s}^2\\) Mars gravitational parameter (\\(\\mu_{\\text{Mars}}\\)) <code>R_MARS</code> <code>3396190.0</code> \\(\\text{m}\\) Mars equatorial radius <code>G_VENUS</code> <code>3.24859e14</code> \\(\\text{m}^3 / \\text{s}^2\\) Venus gravitational parameter (\\(\\mu_{\\text{Venus}}\\)) <code>R_VENUS</code> <code>6051800.0</code> \\(\\text{m}\\) Venus mean planetary radius <code>G_JUPITER</code> <code>1.26686534e17</code> \\(\\text{m}^3 / \\text{s}^2\\) Jupiter gravitational parameter (\\(\\mu_{\\text{Jupiter}}\\)) <code>R_JUPITER</code> <code>71492000.0</code> \\(\\text{m}\\) Jupiter equatorial radius"},{"location":"reference/foundations/constants/#core.constants--geopotential-zonal-harmonics-egm96-wgs-84","title":"Geopotential Zonal Harmonics (EGM96 / WGS-84)","text":"Constant Value Units Description <code>J2_EARTH</code> <code>1.08262668e-3</code> \u2014 Unnormalized second zonal harmonic (oblateness) <code>J3_EARTH</code> <code>-2.53265649e-6</code> \u2014 Unnormalized third zonal harmonic (pear shape) <code>J4_EARTH</code> <code>-1.61962160e-6</code> \u2014 Unnormalized fourth zonal harmonic"},{"location":"reference/foundations/constants/#core.constants--atmosphere-radiation-heliocentric-orbits","title":"Atmosphere, Radiation &amp; Heliocentric Orbits","text":"Constant Value Units Description <code>RHO_0</code> <code>1.225</code> \\(\\text{kg} / \\text{m}^3\\) Sea-level atmospheric reference density (\\(\\rho_0\\)) <code>SCALE_HEIGHT</code> <code>8500.0</code> \\(\\text{m}\\) Atmospheric density scale height (\\(H\\)) <code>P_SUN_1AU</code> <code>4.56e-6</code> \\(\\text{N} / \\text{m}^2\\) Solar radiation pressure flux at 1 AU <code>R_ORBIT_EARTH</code> <code>1.000 * AU</code> \\(\\text{m}\\) Mean orbital radius of Earth <code>R_ORBIT_MARS</code> <code>1.524 * AU</code> \\(\\text{m}\\) Mean orbital radius of Mars <code>R_ORBIT_VENUS</code> <code>0.723 * AU</code> \\(\\text{m}\\) Mean orbital radius of Venus <code>R_ORBIT_JUPITER</code> <code>5.204 * AU</code> \\(\\text{m}\\) Mean orbital radius of Jupiter"},{"location":"reference/foundations/ephemeris/","title":"ephemeris","text":""},{"location":"reference/foundations/ephemeris/#core.ephemeris","title":"core.ephemeris","text":"<p>High-accuracy Keplerian analytical planetary ephemeris.</p> <p>Provides analytical planetary state vectors and orbital geometry based on Standish / JPL secular variations of planetary elements (valid 1800\u20132050).</p>"},{"location":"reference/foundations/ephemeris/#core.ephemeris.get_planet_state","title":"get_planet_state","text":"<pre><code>get_planet_state(\n    planet_name: str,\n    mjd2000: float,\n    frame: str = \"ecliptic\",\n) -&gt; Tuple[ndarray, ndarray]\n</code></pre> <p>Compute heliocentric 3D position and velocity vectors.</p> <p>Parameters:</p> Name Type Description Default <code>planet_name</code> <code>str</code> <p>Target celestial body identifier (<code>'mercury'</code>, <code>'venus'</code>, <code>'earth'</code>, <code>'mars'</code>, <code>'jupiter'</code>, <code>'saturn'</code>, <code>'uranus'</code>, <code>'neptune'</code>, or <code>'pluto'</code>).</p> required <code>mjd2000</code> <code>float</code> <p>Days elapsed since J2000.0 epoch (2000-01-01 12:00:00 TT).</p> required <code>frame</code> <code>str</code> <p>Target coordinate frame: * <code>'ecliptic'</code>: Heliocentric Mean Ecliptic and Equinox of J2000.0. * <code>'equatorial'</code>: Heliocentric ICRF / J2000.0 frame.</p> <code>'ecliptic'</code> <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>Tuple[np.ndarray, np.ndarray]: A tuple <code>(r_vec, v_vec)</code> containing: - r_vec (<code>np.ndarray</code>): Position vector \\([x, y, z]\\) of shape <code>(3,)</code> [\\(\\text{km}\\)]. - v_vec (<code>np.ndarray</code>): Velocity vector \\([v_x, v_y, v_z]\\) of shape <code>(3,)</code> [\\(\\text{km/s}\\)].</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If <code>planet_name</code> is not in the planetary database.</p> Source code in <code>core/ephemeris.py</code> <pre><code>def get_planet_state(\n    planet_name: str,\n    mjd2000: float,\n    frame: str = 'ecliptic'\n) -&gt; Tuple[np.ndarray, np.ndarray]:\n    r\"\"\"Compute heliocentric 3D position and velocity vectors.\n\n    Args:\n        planet_name: Target celestial body identifier (`'mercury'`, `'venus'`,\n            `'earth'`, `'mars'`, `'jupiter'`, `'saturn'`, `'uranus'`,\n            `'neptune'`, or `'pluto'`).\n        mjd2000: Days elapsed since J2000.0 epoch (2000-01-01 12:00:00 TT).\n        frame: Target coordinate frame:\n            * `'ecliptic'`: Heliocentric Mean Ecliptic and Equinox of J2000.0.\n            * `'equatorial'`: Heliocentric ICRF / J2000.0 frame.\n\n    Returns:\n        Tuple[np.ndarray, np.ndarray]: A tuple `(r_vec, v_vec)` containing:\n            - **r_vec** (`np.ndarray`): Position vector $[x, y, z]$ of shape `(3,)` [$\\text{km}$].\n            - **v_vec** (`np.ndarray`): Velocity vector $[v_x, v_y, v_z]$ of shape `(3,)` [$\\text{km/s}$].\n\n    Raises:\n        KeyError: If `planet_name` is not in the planetary database.\n    \"\"\"\n    p = PLANET_DATA[planet_name.lower()]\n    t_centuries = mjd2000 / 36525.0\n\n    elem = [b + r * t_centuries for b, r in zip(p['base'], p['rates'])]\n    a = elem[0] * AU\n    e = elem[1]\n    inc = np.radians(elem[2])\n    raan = np.radians(elem[3])\n    varpi = np.radians(elem[4])\n    mean_long = np.radians(elem[5])\n\n    omega = (varpi - raan) % (2.0 * np.pi)\n    m_anom = (mean_long - varpi) % (2.0 * np.pi)\n\n    e_anom = m_anom if e &lt; 0.8 else np.pi\n    for _ in range(15):\n        f = e_anom - e * np.sin(e_anom) - m_anom\n        f_prime = 1.0 - e * np.cos(e_anom)\n        d_e = -f / f_prime\n        e_anom += d_e\n        if abs(d_e) &lt; 1e-12:\n            break\n\n    nu = 2.0 * np.arctan2(\n        np.sqrt(1.0 + e) * np.sin(e_anom / 2.0),\n        np.sqrt(1.0 - e) * np.cos(e_anom / 2.0)\n    )\n    r_mag = a * (1.0 - e * np.cos(e_anom))\n\n    r_pf = np.array([r_mag * np.cos(nu), r_mag * np.sin(nu), 0.0])\n    p_orb = a * (1.0 - e ** 2)\n    h = np.sqrt(MU_SUN * p_orb)\n    v_pf = np.array([\n        -(MU_SUN / h) * np.sin(nu),\n        (MU_SUN / h) * (e + np.cos(nu)),\n        0.0\n    ])\n\n    p_vec = np.array([\n        np.cos(raan) * np.cos(omega) - np.sin(raan) * np.sin(omega) * np.cos(inc),\n        np.sin(raan) * np.cos(omega) + np.cos(raan) * np.sin(omega) * np.cos(inc),\n        np.sin(omega) * np.sin(inc)\n    ])\n\n    q_vec = np.array([\n        -np.cos(raan) * np.sin(omega) - np.sin(raan) * np.cos(omega) * np.cos(inc),\n        -np.sin(raan) * np.sin(omega) + np.cos(raan) * np.cos(omega) * np.cos(inc),\n        np.cos(omega) * np.sin(inc)\n    ])\n\n    w_vec = np.cross(p_vec, q_vec)\n    r_pf2ecl = np.column_stack([p_vec, q_vec, w_vec])\n\n    r_ecl = r_pf2ecl @ r_pf\n    v_ecl = r_pf2ecl @ v_pf\n\n    if frame.lower() == 'equatorial':\n        r_eq = np.array([\n            [1.0, 0.0, 0.0],\n            [0.0, np.cos(OBLIQUITY_J2000), -np.sin(OBLIQUITY_J2000)],\n            [0.0, np.sin(OBLIQUITY_J2000), np.cos(OBLIQUITY_J2000)]\n        ])\n        return r_eq @ r_ecl, r_eq @ v_ecl\n\n    return r_ecl, v_ecl\n</code></pre>"},{"location":"reference/foundations/ephemeris/#core.ephemeris.get_soi_radius","title":"get_soi_radius","text":"<pre><code>get_soi_radius(planet_name: str) -&gt; float\n</code></pre> <p>Compute Laplace Sphere of Influence (SOI) radius.</p> \\[ r_{\\text{SOI}} = a \\left(\\frac{m}{M}\\right)^{0.4} \\] <p>Parameters:</p> Name Type Description Default <code>planet_name</code> <code>str</code> <p>Target celestial body identifier (e.g., <code>'earth'</code>, <code>'mars'</code>).</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Sphere of influence radius in \\(\\text{km}\\).</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>If <code>planet_name</code> is not in the planetary database.</p> Source code in <code>core/ephemeris.py</code> <pre><code>def get_soi_radius(planet_name: str) -&gt; float:\n    r\"\"\"Compute Laplace Sphere of Influence (SOI) radius.\n\n    $$\n    r_{\\text{SOI}} = a \\left(\\frac{m}{M}\\right)^{0.4}\n    $$\n\n    Args:\n        planet_name: Target celestial body identifier (e.g., `'earth'`, `'mars'`).\n\n    Returns:\n        float: Sphere of influence radius in $\\text{km}$.\n\n    Raises:\n        KeyError: If `planet_name` is not in the planetary database.\n    \"\"\"\n    p = PLANET_DATA[planet_name.lower()]\n    return float(p['soi_a'] * (p['mass_ratio']) ** 0.4)\n</code></pre>"},{"location":"reference/foundations/time/","title":"time","text":""},{"location":"reference/foundations/time/#core.time","title":"core.time","text":"<p>Time &amp; Epoch Conversion Utilities for Astrodynamics.</p> <p>Supports conversions between UTC Gregorian datetime, Julian Date (JD), Modified Julian Date (MJD), and J2000.0 epoch offsets.</p>"},{"location":"reference/foundations/time/#core.time.datetime_to_jd","title":"datetime_to_jd","text":"<pre><code>datetime_to_jd(dt: datetime) -&gt; float\n</code></pre> <p>Convert a Python UTC datetime object to Julian Date (JD).</p> <p>Parameters:</p> Name Type Description Default <code>dt</code> <code>datetime</code> <p>UTC Calendar datetime.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Julian Date in fractional days.</p> Source code in <code>core/time.py</code> <pre><code>def datetime_to_jd(dt: datetime) -&gt; float:\n    r\"\"\"Convert a Python UTC datetime object to Julian Date (JD).\n\n    Args:\n        dt: UTC Calendar datetime.\n\n    Returns:\n        float: Julian Date in fractional days.\n    \"\"\"\n    year = dt.year\n    month = dt.month\n    day = dt.day\n\n    frac_day = (dt.hour + dt.minute / 60.0 + (dt.second + dt.microsecond * 1e-6) / 3600.0) / 24.0\n\n    if month &lt;= 2:\n        year -= 1\n        month += 12\n\n    a = year // 100\n    b = 2 - a + (a // 4)\n\n    jd = int(365.25 * (year + 4716)) + int(30.6001 * (month + 1)) + day + b - 1524.5 + frac_day\n    return float(jd)\n</code></pre>"},{"location":"reference/foundations/time/#core.time.jd_to_datetime","title":"jd_to_datetime","text":"<pre><code>jd_to_datetime(jd: float) -&gt; datetime\n</code></pre> <p>Convert Julian Date (JD) back to a Python UTC datetime object.</p> <p>Parameters:</p> Name Type Description Default <code>jd</code> <code>float</code> <p>Julian Date in fractional days.</p> required <p>Returns:</p> Name Type Description <code>datetime</code> <code>datetime</code> <p>UTC Calendar datetime representation.</p> Source code in <code>core/time.py</code> <pre><code>def jd_to_datetime(jd: float) -&gt; datetime:\n    r\"\"\"Convert Julian Date (JD) back to a Python UTC datetime object.\n\n    Args:\n        jd: Julian Date in fractional days.\n\n    Returns:\n        datetime: UTC Calendar datetime representation.\n    \"\"\"\n    jd_adjusted = jd + 0.5\n    z = int(jd_adjusted)\n    f = jd_adjusted - z\n\n    if z &lt; 2299161:\n        a = z\n    else:\n        alpha = int((z - 1867216.25) / 36524.25)\n        a = z + 1 + alpha - (alpha // 4)\n\n    b = a + 1524\n    c = int((b - 122.1) / 365.25)\n    d = int(365.25 * c)\n    e = int((b - d) / 30.6001)\n\n    day_float = b - d - int(30.6001 * e) + f\n    day = int(day_float)\n    frac_day = day_float - day\n\n    if e &lt; 14:\n        month = e - 1\n    else:\n        month = e - 13\n\n    if month &gt; 2:\n        year = c - 4716\n    else:\n        year = c - 4715\n\n    total_seconds = frac_day * SECS_PER_DAY\n    hours = int(total_seconds // 3600)\n    total_seconds %= 3600\n    minutes = int(total_seconds // 60)\n    seconds_float = total_seconds % 60\n    seconds = int(seconds_float)\n    microseconds = int(round((seconds_float - seconds) * 1e6))\n\n    if microseconds &gt;= 1_000_000:\n        microseconds -= 1_000_000\n        seconds += 1\n\n    return datetime(year, month, day, hours, minutes, seconds, microseconds)\n</code></pre>"},{"location":"reference/foundations/time/#core.time.date_to_mjd2000","title":"date_to_mjd2000","text":"<pre><code>date_to_mjd2000(\n    date_val: Union[datetime, str, float],\n) -&gt; float\n</code></pre> <p>Convert input date representation to days elapsed since J2000.0 (MJD2000).</p> <p>Parameters:</p> Name Type Description Default <code>date_val</code> <code>Union[datetime, str, float]</code> <p>Date representation to parse: * <code>datetime</code>: Interpreted directly as UTC datetime. * <code>str</code>: Parsed as an ISO 8601 string (e.g., <code>'2026-08-28 12:00:00'</code>). * <code>float</code>: Treated as an existing Julian Date (JD).</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Days elapsed since J2000.0 epoch (2000-01-01 12:00:00 TT).</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If <code>date_val</code> is not a datetime, ISO string, or float.</p> Source code in <code>core/time.py</code> <pre><code>def date_to_mjd2000(date_val: Union[datetime, str, float]) -&gt; float:\n    r\"\"\"Convert input date representation to days elapsed since J2000.0 (MJD2000).\n\n    Args:\n        date_val: Date representation to parse:\n            * `datetime`: Interpreted directly as UTC datetime.\n            * `str`: Parsed as an ISO 8601 string (e.g., `'2026-08-28 12:00:00'`).\n            * `float`: Treated as an existing Julian Date (JD).\n\n    Returns:\n        float: Days elapsed since J2000.0 epoch (2000-01-01 12:00:00 TT).\n\n    Raises:\n        TypeError: If `date_val` is not a datetime, ISO string, or float.\n    \"\"\"\n    if isinstance(date_val, str):\n        dt = datetime.fromisoformat(date_val)\n        jd = datetime_to_jd(dt)\n    elif isinstance(date_val, datetime):\n        jd = datetime_to_jd(date_val)\n    elif isinstance(date_val, (int, float)):\n        jd = float(date_val)\n    else:\n        raise TypeError(f\"Unsupported date format: {type(date_val)}\")\n\n    return jd - JD_J2000\n</code></pre>"},{"location":"reference/foundations/time/#core.time.mjd2000_to_datetime","title":"mjd2000_to_datetime","text":"<pre><code>mjd2000_to_datetime(mjd2000: float) -&gt; datetime\n</code></pre> <p>Convert days since J2000.0 (MJD2000) to Python UTC datetime.</p> <p>Parameters:</p> Name Type Description Default <code>mjd2000</code> <code>float</code> <p>Days elapsed since J2000.0 epoch.</p> required <p>Returns:</p> Name Type Description <code>datetime</code> <code>datetime</code> <p>UTC Calendar datetime representation.</p> Source code in <code>core/time.py</code> <pre><code>def mjd2000_to_datetime(mjd2000: float) -&gt; datetime:\n    r\"\"\"Convert days since J2000.0 (MJD2000) to Python UTC datetime.\n\n    Args:\n        mjd2000: Days elapsed since J2000.0 epoch.\n\n    Returns:\n        datetime: UTC Calendar datetime representation.\n    \"\"\"\n    jd = mjd2000 + JD_J2000\n    return jd_to_datetime(jd)\n</code></pre>"},{"location":"reference/foundations/time/#core.time.mjd2000_to_julian_centuries","title":"mjd2000_to_julian_centuries","text":"<pre><code>mjd2000_to_julian_centuries(mjd2000: float) -&gt; float\n</code></pre> <p>Convert days since J2000.0 to Julian centuries (\\(T\\)).</p> <p>Parameters:</p> Name Type Description Default <code>mjd2000</code> <code>float</code> <p>Days elapsed since J2000.0 epoch.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Julian centuries (\\(T\\)) elapsed since J2000.0.</p> Source code in <code>core/time.py</code> <pre><code>def mjd2000_to_julian_centuries(mjd2000: float) -&gt; float:\n    r\"\"\"Convert days since J2000.0 to Julian centuries ($T$).\n\n    Args:\n        mjd2000: Days elapsed since J2000.0 epoch.\n\n    Returns:\n        float: Julian centuries ($T$) elapsed since J2000.0.\n    \"\"\"\n    return mjd2000 / DAYS_PER_JULIAN_CENTURY\n</code></pre>"},{"location":"reference/mission_design/cr3bp/","title":"cr3bp","text":""},{"location":"reference/mission_design/cr3bp/#core.cr3bp","title":"core.cr3bp","text":"<p>Circular Restricted Three-Body Problem (CR3BP) Engine.</p> <p>Supports non-dimensional synodic equations of motion, Jacobi energy constant calculations, Lagrange libration point (\\(L_1\\)-\\(L_5\\)) solvers, and coordinate transformations into dimensional inertial reference frames.</p>"},{"location":"reference/mission_design/cr3bp/#core.cr3bp.cr3bp_derivatives","title":"cr3bp_derivatives","text":"<pre><code>cr3bp_derivatives(\n    t: float, state: ndarray, mu: float = MU_EARTH_MOON\n) -&gt; ndarray\n</code></pre> <p>Evaluate equations of motion in the non-dimensional synodic rotating frame.</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>float</code> <p>Non-dimensional time parameter \\(\\tau\\).</p> required <code>state</code> <code>ndarray</code> <p>Non-dimensional state vector \\([x, y, z, \\dot{x}, \\dot{y}, \\dot{z}]\\) of shape <code>(6,)</code>.</p> required <code>mu</code> <code>float</code> <p>Primary mass ratio \\(\\mu = \\frac{m_2}{m_1 + m_2}\\). Defaults to <code>MU_EARTH_MOON</code>.</p> <code>MU_EARTH_MOON</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: State derivatives \\([\\dot{x}, \\dot{y}, \\dot{z}, \\ddot{x}, \\ddot{y}, \\ddot{z}]\\) of shape <code>(6,)</code>.</p> Source code in <code>core/cr3bp.py</code> <pre><code>def cr3bp_derivatives(t: float, state: np.ndarray, mu: float = MU_EARTH_MOON) -&gt; np.ndarray:\n    r\"\"\"Evaluate equations of motion in the non-dimensional synodic rotating frame.\n\n    Args:\n        t: Non-dimensional time parameter $\\tau$.\n        state: Non-dimensional state vector $[x, y, z, \\dot{x}, \\dot{y}, \\dot{z}]$ of shape `(6,)`.\n        mu: Primary mass ratio $\\mu = \\frac{m_2}{m_1 + m_2}$. Defaults to `MU_EARTH_MOON`.\n\n    Returns:\n        np.ndarray: State derivatives $[\\dot{x}, \\dot{y}, \\dot{z}, \\ddot{x}, \\ddot{y}, \\ddot{z}]$ of shape `(6,)`.\n    \"\"\"\n    x, y, z, vx, vy, vz = state\n\n    r1 = np.sqrt((x + mu) ** 2 + y ** 2 + z ** 2)\n    r2 = np.sqrt((x - 1.0 + mu) ** 2 + y ** 2 + z ** 2)\n\n    omega_x = x - (1.0 - mu) * (x + mu) / (r1 ** 3) - mu * (x - 1.0 + mu) / (r2 ** 3)\n    omega_y = y - (1.0 - mu) * y / (r1 ** 3) - mu * y / (r2 ** 3)\n    omega_z = -(1.0 - mu) * z / (r1 ** 3) - mu * z / (r2 ** 3)\n\n    ax = 2.0 * vy + omega_x\n    ay = -2.0 * vx + omega_y\n    az = omega_z\n\n    return np.array([vx, vy, vz, ax, ay, az])\n</code></pre>"},{"location":"reference/mission_design/cr3bp/#core.cr3bp.compute_jacobi_constant","title":"compute_jacobi_constant","text":"<pre><code>compute_jacobi_constant(\n    state: ndarray, mu: float = MU_EARTH_MOON\n) -&gt; float\n</code></pre> <p>Compute the conserved Jacobi Energy Constant (\\(C_J\\)).</p> \\[ C_J = 2\\Omega(x, y, z) - v^2 \\] <p>Parameters:</p> Name Type Description Default <code>state</code> <code>ndarray</code> <p>Non-dimensional synodic state vector \\([x, y, z, \\dot{x}, \\dot{y}, \\dot{z}]\\) of shape <code>(6,)</code>.</p> required <code>mu</code> <code>float</code> <p>Primary mass ratio \\(\\mu\\). Defaults to <code>MU_EARTH_MOON</code>.</p> <code>MU_EARTH_MOON</code> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Conserved Jacobi energy constant \\(C_J\\).</p> Source code in <code>core/cr3bp.py</code> <pre><code>def compute_jacobi_constant(state: np.ndarray, mu: float = MU_EARTH_MOON) -&gt; float:\n    r\"\"\"Compute the conserved Jacobi Energy Constant ($C_J$).\n\n    $$\n    C_J = 2\\Omega(x, y, z) - v^2\n    $$\n\n    Args:\n        state: Non-dimensional synodic state vector $[x, y, z, \\dot{x}, \\dot{y}, \\dot{z}]$ of shape `(6,)`.\n        mu: Primary mass ratio $\\mu$. Defaults to `MU_EARTH_MOON`.\n\n    Returns:\n        float: Conserved Jacobi energy constant $C_J$.\n    \"\"\"\n    x, y, z, vx, vy, vz = state\n    r1 = np.sqrt((x + mu) ** 2 + y ** 2 + z ** 2)\n    r2 = np.sqrt((x - 1.0 + mu) ** 2 + y ** 2 + z ** 2)\n\n    v_sq = vx ** 2 + vy ** 2 + vz ** 2\n    omega = 0.5 * (x ** 2 + y ** 2) + (1.0 - mu) / r1 + mu / r2\n\n    return float(2.0 * omega - v_sq)\n</code></pre>"},{"location":"reference/mission_design/cr3bp/#core.cr3bp.compute_lagrange_points","title":"compute_lagrange_points","text":"<pre><code>compute_lagrange_points(\n    mu: float = MU_EARTH_MOON,\n) -&gt; Dict[str, ndarray]\n</code></pre> <p>Compute non-dimensional coordinates of all five Lagrange equilibrium points.</p> <p>Parameters:</p> Name Type Description Default <code>mu</code> <code>float</code> <p>Primary mass ratio \\(\\mu\\). Defaults to <code>MU_EARTH_MOON</code>.</p> <code>MU_EARTH_MOON</code> <p>Returns:</p> Type Description <code>Dict[str, ndarray]</code> <p>Dict[str, np.ndarray]: Dictionary mapping labels (<code>'L1'</code> through <code>'L5'</code>) to synodic position vectors \\([x, y, z]\\) of shape <code>(3,)</code>.</p> Source code in <code>core/cr3bp.py</code> <pre><code>def compute_lagrange_points(mu: float = MU_EARTH_MOON) -&gt; Dict[str, np.ndarray]:\n    r\"\"\"Compute non-dimensional coordinates of all five Lagrange equilibrium points.\n\n    Args:\n        mu: Primary mass ratio $\\mu$. Defaults to `MU_EARTH_MOON`.\n\n    Returns:\n        Dict[str, np.ndarray]: Dictionary mapping labels (`'L1'` through `'L5'`)\n            to synodic position vectors $[x, y, z]$ of shape `(3,)`.\n    \"\"\"\n    def domega_dx(x):\n        r1 = abs(x + mu)\n        r2 = abs(x - 1.0 + mu)\n        return x - (1.0 - mu) * np.sign(x + mu) / (r1 ** 2) - mu * np.sign(x - 1.0 + mu) / (r2 ** 2)\n\n    l1_x = root_scalar(domega_dx, bracket=[0.0, 1.0 - mu - 1e-4]).root\n    l2_x = root_scalar(domega_dx, bracket=[1.0 - mu + 1e-4, 1.5]).root\n    l3_x = root_scalar(domega_dx, bracket=[-1.5, -mu - 1e-4]).root\n\n    return {\n        \"L1\": np.array([l1_x, 0.0, 0.0]),\n        \"L2\": np.array([l2_x, 0.0, 0.0]),\n        \"L3\": np.array([l3_x, 0.0, 0.0]),\n        \"L4\": np.array([0.5 - mu, np.sqrt(3.0) / 2.0, 0.0]),\n        \"L5\": np.array([0.5 - mu, -np.sqrt(3.0) / 2.0, 0.0]),\n    }\n</code></pre>"},{"location":"reference/mission_design/cr3bp/#core.cr3bp.synodic_to_inertial","title":"synodic_to_inertial","text":"<pre><code>synodic_to_inertial(\n    times_nd: ndarray,\n    states_nd: ndarray,\n    mu: float = MU_EARTH_MOON,\n) -&gt; ndarray\n</code></pre> <p>Transform synodic state trajectories into dimensional ECI coordinates.</p> <p>Parameters:</p> Name Type Description Default <code>times_nd</code> <code>ndarray</code> <p>Array of non-dimensional time steps of shape <code>(N,)</code>.</p> required <code>states_nd</code> <code>ndarray</code> <p>Array of non-dimensional synodic states of shape <code>(N, 6)</code>.</p> required <code>mu</code> <code>float</code> <p>Primary mass ratio \\(\\mu\\). Defaults to <code>MU_EARTH_MOON</code>.</p> <code>MU_EARTH_MOON</code> <p>Returns:</p> Type Description <code>ndarray</code> <p>np.ndarray: Dimensional ECI state vectors of shape <code>(N, 6)</code> in \\([\\text{km}, \\text{km/s}]\\).</p> Source code in <code>core/cr3bp.py</code> <pre><code>def synodic_to_inertial(\n    times_nd: np.ndarray,\n    states_nd: np.ndarray,\n    mu: float = MU_EARTH_MOON\n) -&gt; np.ndarray:\n    r\"\"\"Transform synodic state trajectories into dimensional ECI coordinates.\n\n    Args:\n        times_nd: Array of non-dimensional time steps of shape `(N,)`.\n        states_nd: Array of non-dimensional synodic states of shape `(N, 6)`.\n        mu: Primary mass ratio $\\mu$. Defaults to `MU_EARTH_MOON`.\n\n    Returns:\n        np.ndarray: Dimensional ECI state vectors of shape `(N, 6)` in $[\\text{km}, \\text{km/s}]$.\n    \"\"\"\n    num_steps = len(times_nd)\n    states_eci_km = np.zeros((num_steps, 6))\n    earth_offset_nd = np.array([-mu, 0.0, 0.0])\n\n    for i in range(num_steps):\n        t = times_nd[i]\n        theta = t\n\n        cos_t, sin_t = np.cos(theta), np.sin(theta)\n\n        r_rot = np.array([\n            [cos_t, -sin_t, 0.0],\n            [sin_t,  cos_t, 0.0],\n            [0.0,    0.0,   1.0]\n        ])\n\n        r_dot = np.array([\n            [-sin_t, -cos_t, 0.0],\n            [ cos_t, -sin_t, 0.0],\n            [ 0.0,    0.0,   0.0]\n        ])\n\n        r_syn = states_nd[i, 0:3] - earth_offset_nd\n        v_syn = states_nd[i, 3:6]\n\n        r_eci_nd = r_rot @ r_syn\n        v_eci_nd = r_rot @ v_syn + r_dot @ r_syn\n\n        states_eci_km[i, 0:3] = r_eci_nd * (L_STAR / 1000.0)\n        states_eci_km[i, 3:6] = v_eci_nd * (V_STAR / 1000.0)\n\n    return states_eci_km\n</code></pre>"},{"location":"reference/mission_design/flyby/","title":"flyby","text":""},{"location":"reference/mission_design/flyby/#core.flyby","title":"core.flyby","text":"<p>Hyperbolic gravity assist (flyby) dynamics and patched-conic matching engine.</p> <p>Evaluates turning angles, periapsis altitudes, and required powered flyby Delta-Vs.</p>"},{"location":"reference/mission_design/flyby/#core.flyby.evaluate_unpowered_flyby","title":"evaluate_unpowered_flyby","text":"<pre><code>evaluate_unpowered_flyby(\n    v_inf_in: ndarray,\n    v_inf_out: ndarray,\n    planet_name: str,\n    min_altitude_km: float = 300.0,\n) -&gt; Dict[str, Any]\n</code></pre> <p>Evaluate an unpowered hyperbolic gravity assist maneuver.</p> <p>Parameters:</p> Name Type Description Default <code>v_inf_in</code> <code>ndarray</code> <p>Inbound hyperbolic excess velocity vector \\(\\mathbf{v}_\\infty^-\\) relative to planet [\\(\\text{km/s}\\)].</p> required <code>v_inf_out</code> <code>ndarray</code> <p>Outbound hyperbolic excess velocity vector \\(\\mathbf{v}_\\infty^+\\) relative to planet [\\(\\text{km/s}\\)].</p> required <code>planet_name</code> <code>str</code> <p>Flyby planet key (e.g. <code>'venus'</code>, <code>'earth'</code>, <code>'mars'</code>, <code>'jupiter'</code>).</p> required <code>min_altitude_km</code> <code>float</code> <p>Atmospheric clearance margin above planet radius [\\(\\text{km}\\)]. Defaults to <code>300.0</code>.</p> <code>300.0</code> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Flyby evaluation parameters containing: * <code>is_feasible</code> (bool): Unpowered geometric and altitude feasibility. * <code>delta_angle_deg</code> (float): Geometric turn angle \\(\\delta\\) [\\(\\text{deg}\\)]. * <code>v_inf_in_mag</code> (float): Inbound excess speed \\(\\|\\mathbf{v}_\\infty^-\\|\\) [\\(\\text{km/s}\\)]. * <code>v_inf_out_mag</code> (float): Outbound excess speed \\(\\|\\mathbf{v}_\\infty^+\\|\\) [\\(\\text{km/s}\\)]. * <code>dv_powered</code> (float): Deficit \\(\\Delta V\\) required if speeds mismatch [\\(\\text{km/s}\\)]. * <code>r_p</code> (float): Computed periapsis radius [\\(\\text{km}\\)]. * <code>h_p</code> (float): Computed periapsis altitude above surface (\\(r_p - R_p\\)) [\\(\\text{km}\\)].</p> Source code in <code>core/flyby.py</code> <pre><code>def evaluate_unpowered_flyby(\n    v_inf_in: np.ndarray,\n    v_inf_out: np.ndarray,\n    planet_name: str,\n    min_altitude_km: float = 300.0\n) -&gt; Dict[str, Any]:\n    r\"\"\"Evaluate an unpowered hyperbolic gravity assist maneuver.\n\n    Args:\n        v_inf_in: Inbound hyperbolic excess velocity vector $\\mathbf{v}_\\infty^-$\n            relative to planet [$\\text{km/s}$].\n        v_inf_out: Outbound hyperbolic excess velocity vector $\\mathbf{v}_\\infty^+$\n            relative to planet [$\\text{km/s}$].\n        planet_name: Flyby planet key (e.g. `'venus'`, `'earth'`, `'mars'`, `'jupiter'`).\n        min_altitude_km: Atmospheric clearance margin above planet radius [$\\text{km}$]. Defaults to `300.0`.\n\n    Returns:\n        Dict[str, Any]: Flyby evaluation parameters containing:\n            * `is_feasible` (*bool*): Unpowered geometric and altitude feasibility.\n            * `delta_angle_deg` (*float*): Geometric turn angle $\\delta$ [$\\text{deg}$].\n            * `v_inf_in_mag` (*float*): Inbound excess speed $\\|\\mathbf{v}_\\infty^-\\|$ [$\\text{km/s}$].\n            * `v_inf_out_mag` (*float*): Outbound excess speed $\\|\\mathbf{v}_\\infty^+\\|$ [$\\text{km/s}$].\n            * `dv_powered` (*float*): Deficit $\\Delta V$ required if speeds mismatch [$\\text{km/s}$].\n            * `r_p` (*float*): Computed periapsis radius [$\\text{km}$].\n            * `h_p` (*float*): Computed periapsis altitude above surface ($r_p - R_p$) [$\\text{km}$].\n    \"\"\"\n    p_info = PLANET_DATA[planet_name.lower()]\n    mu_p = p_info['mu']\n    r_body = p_info['radius']\n\n    v_in_mag = float(np.linalg.norm(v_inf_in))\n    v_out_mag = float(np.linalg.norm(v_inf_out))\n\n    cos_delta = float(np.dot(v_inf_in, v_inf_out) / (v_in_mag * v_out_mag))\n    cos_delta = float(np.clip(cos_delta, -1.0, 1.0))\n    delta = np.arccos(cos_delta)\n\n    dv_powered = abs(v_out_mag - v_in_mag)\n    v_inf_avg = 0.5 * (v_in_mag + v_out_mag)\n\n    sin_half_delta = np.sin(delta / 2.0)\n\n    if sin_half_delta &lt;= 1e-6:\n        r_p = np.inf\n        h_p = np.inf\n        is_feasible = True\n    else:\n        e = 1.0 / sin_half_delta\n        r_p = (mu_p / (v_inf_avg ** 2)) * (e - 1.0)\n        h_p = r_p - r_body\n        is_feasible = (h_p &gt;= min_altitude_km) and (dv_powered &lt; 0.25)\n\n    return {\n        'is_feasible': bool(is_feasible),\n        'delta_angle_deg': float(np.degrees(delta)),\n        'v_inf_in_mag': float(v_in_mag),\n        'v_inf_out_mag': float(v_out_mag),\n        'dv_powered': float(dv_powered),\n        'r_p': float(r_p),\n        'h_p': float(h_p)\n    }\n</code></pre>"},{"location":"reference/mission_design/lambert/","title":"lambert","text":""},{"location":"reference/mission_design/lambert/#core.lambert","title":"core.lambert","text":"<p>Universal Variables Lambert Boundary Value Problem Solver.</p> <p>Implements the Bate-Mueller-White universal variable formulation with canonical scaling and root-bounding for transfer orbits (elliptical, parabolic, hyperbolic).</p>"},{"location":"reference/mission_design/lambert/#core.lambert.solve_lambert","title":"solve_lambert","text":"<pre><code>solve_lambert(\n    r1: ndarray,\n    r2: ndarray,\n    tof: float,\n    mu: float,\n    prograde: bool = True,\n    max_iter: int = 100,\n    tol: float = 1e-10,\n) -&gt; Tuple[ndarray, ndarray]\n</code></pre> <p>Solve the two-point boundary value Lambert orbital transfer problem.</p> <p>Parameters:</p> Name Type Description Default <code>r1</code> <code>ndarray</code> <p>Initial position vector \\(\\mathbf{r}_1\\) of shape <code>(3,)</code> [\\(\\text{m}\\) or \\(\\text{km}\\)].</p> required <code>r2</code> <code>ndarray</code> <p>Target position vector \\(\\mathbf{r}_2\\) of shape <code>(3,)</code> [\\(\\text{m}\\) or \\(\\text{km}\\)].</p> required <code>tof</code> <code>float</code> <p>Transfer time of flight \\(\\Delta t\\) in seconds. Must be positive.</p> required <code>mu</code> <code>float</code> <p>Gravitational parameter \\(\\mu\\) of central attractor in consistent units.</p> required <code>prograde</code> <code>bool</code> <p>Transfer plane direction. <code>True</code> for short-way (\\(\\Delta \\nu &lt; \\pi\\)), <code>False</code> for retrograde / long-way transfer. Defaults to <code>True</code>.</p> <code>True</code> <code>max_iter</code> <code>int</code> <p>Maximum allowable Newton-Raphson iterations. Defaults to <code>100</code>.</p> <code>100</code> <code>tol</code> <code>float</code> <p>Root-finding convergence tolerance. Defaults to <code>1e-10</code>.</p> <code>1e-10</code> <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>Tuple[np.ndarray, np.ndarray]: A tuple <code>(v1, v2)</code> containing: - v1 (<code>np.ndarray</code>): Departure velocity vector \\(\\mathbf{v}_1\\) of shape <code>(3,)</code>. - v2 (<code>np.ndarray</code>): Arrival velocity vector \\(\\mathbf{v}_2\\) of shape <code>(3,)</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If \\(180^\\circ\\) collinear singularity is encountered or if the solver fails to converge within <code>max_iter</code>.</p> Source code in <code>core/lambert.py</code> <pre><code>def solve_lambert(\n    r1: np.ndarray,\n    r2: np.ndarray,\n    tof: float,\n    mu: float,\n    prograde: bool = True,\n    max_iter: int = 100,\n    tol: float = 1e-10\n) -&gt; Tuple[np.ndarray, np.ndarray]:\n    r\"\"\"Solve the two-point boundary value Lambert orbital transfer problem.\n\n    Args:\n        r1: Initial position vector $\\mathbf{r}_1$ of shape `(3,)` [$\\text{m}$ or $\\text{km}$].\n        r2: Target position vector $\\mathbf{r}_2$ of shape `(3,)` [$\\text{m}$ or $\\text{km}$].\n        tof: Transfer time of flight $\\Delta t$ in seconds. Must be positive.\n        mu: Gravitational parameter $\\mu$ of central attractor in consistent units.\n        prograde: Transfer plane direction. `True` for short-way ($\\Delta \\nu &lt; \\pi$),\n            `False` for retrograde / long-way transfer. Defaults to `True`.\n        max_iter: Maximum allowable Newton-Raphson iterations. Defaults to `100`.\n        tol: Root-finding convergence tolerance. Defaults to `1e-10`.\n\n    Returns:\n        Tuple[np.ndarray, np.ndarray]: A tuple `(v1, v2)` containing:\n            - **v1** (`np.ndarray`): Departure velocity vector $\\mathbf{v}_1$ of shape `(3,)`.\n            - **v2** (`np.ndarray`): Arrival velocity vector $\\mathbf{v}_2$ of shape `(3,)`.\n\n    Raises:\n        ValueError: If $180^\\circ$ collinear singularity is encountered or if\n            the solver fails to converge within `max_iter`.\n    \"\"\"\n    r_unit = float(np.linalg.norm(r1))\n    v_unit = np.sqrt(mu / r_unit)\n    t_unit = r_unit / v_unit\n\n    r1_nd = np.asarray(r1, dtype=np.float64) / r_unit\n    r2_nd = np.asarray(r2, dtype=np.float64) / r_unit\n    tof_nd = tof / t_unit\n    mu_nd = 1.0\n\n    r1_norm = 1.0\n    r2_norm = float(np.linalg.norm(r2_nd))\n\n    cos_dtheta = np.clip(np.dot(r1_nd, r2_nd) / (r1_norm * r2_norm), -1.0, 1.0)\n    cross_12 = np.cross(r1_nd, r2_nd)\n\n    if prograde:\n        dtheta = np.arccos(cos_dtheta) if cross_12[2] &gt;= 0.0 else 2.0 * np.pi - np.arccos(cos_dtheta)\n    else:\n        dtheta = np.arccos(cos_dtheta) if cross_12[2] &lt;= 0.0 else 2.0 * np.pi - np.arccos(cos_dtheta)\n\n    sin_dtheta = np.sin(dtheta)\n    a_param = sin_dtheta * np.sqrt(r1_norm * r2_norm / (1.0 - cos_dtheta))\n\n    if abs(a_param) &lt; 1e-12:\n        raise ValueError(\"Collinear singularity (180 degree transfer).\")\n\n    z = 0.0\n    converged = False\n\n    for _ in range(max_iter):\n        cz = max(1e-15, _stumpff_c(z))\n        sz = _stumpff_s(z)\n        yz = r1_norm + r2_norm + a_param * (z * sz - 1.0) / np.sqrt(cz)\n\n        if yz &lt; 0.0:\n            z += 1.0\n            continue\n\n        tof_calc = ((yz / cz)**1.5 * sz + a_param * np.sqrt(yz)) / np.sqrt(mu_nd)\n        f_val = tof_calc - tof_nd\n\n        if abs(f_val) &lt; tol:\n            converged = True\n            break\n\n        dz = 1e-5\n        cz_plus = max(1e-15, _stumpff_c(z + dz))\n        sz_plus = _stumpff_s(z + dz)\n        yz_plus = r1_norm + r2_norm + a_param * ((z + dz) * sz_plus - 1.0) / np.sqrt(cz_plus)\n\n        if yz_plus &lt; 0.0:\n            dt_dz = 1e-8\n        else:\n            t_plus = ((yz_plus / cz_plus)**1.5 * sz_plus + a_param * np.sqrt(yz_plus)) / np.sqrt(mu_nd)\n            dt_dz = (t_plus - tof_calc) / dz\n\n        if abs(dt_dz) &lt; 1e-15:\n            break\n\n        step = f_val / dt_dz\n        step = max(-50.0, min(50.0, step))\n        z = z - step\n        z = max(-10000.0, min(39.4, z))\n\n    if not converged:\n        raise ValueError(\"Lambert solver failed to converge.\")\n\n    yz = r1_norm + r2_norm + a_param * (z * _stumpff_s(z) - 1.0) / np.sqrt(max(1e-15, _stumpff_c(z)))\n    f = 1.0 - yz / r1_norm\n    g = a_param * np.sqrt(yz / mu_nd)\n    gdot = 1.0 - yz / r2_norm\n\n    v1_nd = (r2_nd - f * r1_nd) / g\n    v2_nd = (gdot * r2_nd - r1_nd) / g\n\n    return v1_nd * v_unit, v2_nd * v_unit\n</code></pre>"}]}